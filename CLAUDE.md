# 心数 / Sense · 项目开发规范（CLAUDE.md）

> 本文件是本仓库的开发指南，供 Claude 与所有开发者遵循。
> 产品需求以 `Sense-PRD.md` 为准，UI 以 `design/` 原型为准，本文件规定「怎么落地」。

---

## 一、项目是什么

**心数 / Sense** —— 一款简洁轻量、支持多人协作与多币种、内置 AI 助手的**微信小程序记账应用**，基于**微信云开发（WeChat Cloud Development）**架构，无独立后端服务器。

**当前状态（重要）**：`miniprogram/` 与 `cloudfunctions/` 目前仍是微信云开发官方 **quickstart 模板**（`pages/index` 是云能力演示页，`quickstartFunctions` 是 sales 示例 CRUD）。**Sense 的真实业务代码尚未开始**。开发即是在此模板基础上，按 PRD 与原型逐步替换为真实业务。

---

## 二、技术栈与架构

- **前端**：微信原生小程序（WXML / WXSS / JS / JSON 四件套），非 uni-app / Taro 等框架。
  - **图表**：ECharts（`miniprogram/ec-canvas/` 定制包，canvas 2d 同层渲染，仅含所需模块）；统计页所有图表统一走 ECharts，不再手写 SVG 图表。
- **后端**：微信云开发
  - **云函数**（`cloudfunctions/`）：Node.js + `wx-server-sdk`，承载业务逻辑与鉴权。
  - **云数据库**：JSON 文档型数据库（`cloud.database()`）。
  - **云存储**：图片附件（收据、记录图片）、导出文件。
  - **AI 能力**：云开发大模型接入，用于 AI 助手（数据问答 / 收据识别 / 自然语言记账）。
- **基础库**：`libVersion` 见 `project.config.json`；云能力要求 ≥ 2.2.3。
- **AppID**：见 `project.config.json` 的 `appid` 字段。

---

## 三、目录结构

```
sense/
├── miniprogram/              # 小程序前端（miniprogramRoot）
│   ├── app.js                # 全局：onLaunch 里 wx.cloud.init，env 需配置
│   ├── app.json              # 全局页面路由、tabBar、窗口配置
│   ├── app.wxss              # 全局样式
│   ├── pages/                # 页面（每页 4 文件：.js/.wxml/.wxss/.json）
│   ├── components/           # 自定义组件
│   ├── images/               # 静态图片与图标
│   └── envList.js            # 云环境列表
├── cloudfunctions/           # 云函数（cloudfunctionRoot）
│   └── quickstartFunctions/  # 模板云函数（待按业务重构/新增）
├── design/                   # ⚠️ 只读：HTML 高保真原型（UI 事实来源）
│   ├── css/app.css           # 设计令牌与组件样式
│   ├── *.html                # 10 个原型页面
│   └── DESIGN-*.{md,json}    # 设计交付说明与清单
├── Sense-PRD.md              # ⚠️ 产品需求文档（功能事实来源）
└── project.config.json       # 项目配置
```

---

## 四、绝对边界（不要碰）

1. **`design/` 目录只读**：仅供阅读参考，任何情况下都不要修改其中的 HTML / CSS / MD / JSON。
2. **不改设计令牌的值**：`design/css/app.css` 的 `:root` 令牌（颜色/字号/间距/圆角/阴影/动效）是视觉契约，移植时**逐字对应**到小程序，不新增、不改值。
3. **不要把原型外壳搬进小程序**：`.stage` / `.phone` / `.dynamic-island` / `.statusbar` / `.home-indicator` 是 iPhone 画框模拟，属于原型 chrome，**不进小程序**。小程序里状态栏由系统提供，导航由 `tabBar` 提供。
4. **不臆造范围**：PRD「待定问题清单」中的点未拍板前不实现；P2/P3 功能不提前混入 P1。

---

## 五、从原型（HTML/CSS）到小程序（WXML/WXSS）的移植规则

原型是「视觉契约」：像素、间距、状态、交互以原型为准；冲突时先对齐原型，再重构内部实现。

> **动手前先调用 skill `/html-to-miniprogram`**：它固化了完整转换流程、常见陷阱与对策（顶部状态栏/导航栏、底部 tabBar、内联 SVG、`color-mix`/毛玻璃降级、px→rpx、原生组件层级、拖拽交互等）以及完成前自检清单。本节是要点摘要，细节以该 skill 为准。

### 5.1 令牌先行
先把 `design/css/app.css` 的 `:root` 令牌整理进 `app.wxss`（或独立令牌文件），再写组件。颜色令牌大量使用 `color-mix()` 与 `oklab`——若目标机型基础库不支持，需在移植时换算为等效固定色值，但**视觉结果必须一致**。

### 5.2 标签与语义映射
| 原型（HTML） | 小程序（WXML） |
|---|---|
| `<div>` / `<section>` | `<view>` |
| `<span>` / 文本 | `<text>` |
| `<a href>` 跳转 | `<navigator url>` 或 `bindtap` + `wx.navigateTo` |
| `<button>` | `<button>` 或 `<view bindtap>`（注意小程序 button 默认样式） |
| `<img src>` | `<image src mode>` |
| `<input>` / `<textarea>` | `<input>` / `<textarea>` |
| 内联 SVG 图标 | 迁为 `image`（PNG/SVG 资源）或自定义 icon 组件 |
| `aria-selected` / `aria-current` 等状态 | 用 `class` 绑定 + data 驱动 |

### 5.3 单位与布局
- 原型用 `px`；小程序页面尺寸建议改用 **`rpx`**（原型基于 iPhone 15 逻辑宽 393px，`1px ≈ 2rpx` 作为换算起点，再按视觉校准）。字体、边框等细节可保留 `px`。
- 保留原型的 flex / grid 布局与响应式意图；小程序为单栏移动布局，无需 desktop 断点。

### 5.4 屏 ↔ 页对应（screen-file-first）
`design/` 每个 HTML 屏对应小程序一个页面（`index.html` 是原型启动台/总览，**不**对应页面）：

| 原型文件 | 小程序页面（建议） | tabBar | PRD 章节 |
|---|---|---|---|
| `onboarding.html` | 首次引导 / 建账本 | — | 5.1 |
| `home.html` | 首页（记账列表） | ✅ 首页 | 5.2 |
| `add.html` | 记账录入 | — | 5.3 |
| `detail.html` | 记录详情 | — | 5.4 |
| `stats.html` | 统计图表 | ✅ 统计 | 5.5 |
| `ai.html` | AI 助手 | ✅ AI 助手 | 5.6 |
| `books.html` | 账本 / 成员管理 | — | 5.7 |
| `settings.html` | 我的 / 设置 | ✅ 我的 | 5.8 |
| `settle.html` | 分账结算（**P2**） | — | 4.x/P2 |

### 5.5 交互与状态
逐一还原原型可见的交互态：`default / active / disabled / loading / empty / error / success`，以及分段控件、九宫格分类、拖拽排序、确认卡片、iOS 开关等。原型是静态的，缺失行为按可见控件推导并对齐 PRD 验收标准。

---

## 六、云函数规范

### 6.1 初始化与鉴权
- 统一使用动态环境：`cloud.init({ env: cloud.DYNAMIC_CURRENT_ENV })`，不写死环境 ID。
- 用户身份从上下文取：`cloud.getWXContext().OPENID` 是唯一可信用户标识，**权限校验以服务端 OPENID 为准，绝不信任前端传入的身份**。

### 6.2 入口分发模式（沿用模板约定）
云函数用单入口 + `event.type` 分发（见 `quickstartFunctions/index.js`）：

```js
exports.main = async (event, context) => {
  switch (event.type) {
    case "xxx": return await xxx(event);
    // ...
  }
};
```

- 前端统一 `wx.cloud.callFunction({ name, data: { type, ...payload } })`。
- 业务复杂后按领域拆分云函数（如 `book` / `record` / `ai` / `exportImport`），每个仍保持 type 分发；避免所有逻辑堆在一个函数里。
- **返回结构统一**：成功 `{ success: true, data }`，失败 `{ success: false, errMsg }`（沿用模板风格），前端据此判定。

### 6.3 数据库与权限
- 每次写/读记录，在云函数内校验调用者对该账本的角色是否满足操作（见第七节权限矩阵），**不可依赖前端隐藏按钮来「保护」数据**。
- 「未受邀成员完全不可见某账本」必须在数据查询层强制（按成员关系过滤），不能只在 UI 层隐藏。

### 6.4 依赖与部署
- 每个云函数有独立 `package.json`；新增依赖后需「上传并部署 - 云端安装依赖」。
- 部署：微信开发者工具中对云函数目录右键上传，或参考 `uploadCloudFunction.sh`（`cloudbase functions deploy`）。

---

## 七、关键业务规则（实现时必须遵守）

以下规则直接影响数据模型与代码逻辑，来自 PRD，务必落实：

### 7.1 权限（跟随账本，服务端强制）
四级角色：**owner / admin / 读写 / 只读**，权限随「账本 + 用户」绑定，同一用户在不同账本角色可不同。权限矩阵见 PRD 第三节。要点：
- 每账本有且仅有一个 owner；读写成员只能改/删**自己**的记录，admin/owner 可改全部。
- 只读成员：无「记一笔」入口、不产生入账；可用 AI 数据问答。
- 所有校验在云函数端完成。

### 7.2 多币种（当日汇率固化，历史不变）
- 每笔记录保存**原始币种 + 金额**与**记账当日汇率换算后的展示币种金额**，换算结果**固化**，之后汇率变化历史金额**永不改变**。
- 汇率来自公开每日汇率数据；当日取不到用最近一次并标注。
- 展示币种切换只改变展示汇总口径，不重算历史每笔的当日换算逻辑。
- 记录字段至少含：类型(收/支)、金额、币种、当日汇率、分类、日期、备注、图片、记录人、付款人（分账账本含分摊信息）。

### 7.3 账本类型
- 两种：**共享可见型**（只记录谁付谁记，不算欠款）/ **分账结算型**（记录付款人+分摊）。
- **P1 分账结算型只做「能创建 + 能记录」，自动算谁欠谁的结算逻辑是 P2**（`settle.html` 属 P2）。

### 7.4 分类（账本级、两级、收支分离）
- 分类挂在**账本**上，账本内成员共用同一套，保证统计口径一致。
- 两级分类；收入与支出各有各的集合。
- 删除已用分类建议「停用」而非硬删，历史记录仍显示原分类名。

### 7.5 图表布局（每用户 × 每账本各一套）
- 卡片有哪些、什么顺序，按「账本 + 用户」各存一套，互不影响；下次进入恢复该用户布局。
- 图表共 8 种类型（占比/趋势/对比/结余，详见 PRD 4.5）；默认四卡片：月度收支 / 分类占比 / 支出趋势（折线）/ 收支对比。所有图表只统计当前账本。

### 7.6 AI 助手（绝不自动入账）
- 三能力：数据问答、收据识别、自然语言记账。
- **无论收据识别还是自然语言记账，都只生成「预填记录」，用户确认后才入账**，AI 绝不直接写库。
- 数据问答必须基于账本真实数据，**不允许编造数字**。
- AI 会话按账本分别保留，设上限（默认 50 条，可在设置调整），超出滚动删除最旧。
- AI 能力受当前账本与用户可见范围约束。

### 7.7 导入导出
- 导出：JSON / Excel(含 CSV) / PDF；导入：JSON / Excel(含 CSV)，**PDF 不支持导入**。
- 导入需给出结果反馈（成功/失败条数及原因）。P1 只做符合导出标准格式的基础导入。

---

## 八、编码约定

- **缩进 2 空格**，`insertSpaces`（见 `project.config.json` 的 `editorSetting`）。
- **文件命名**：页面/组件目录用小写，页面通常 `index.js/.wxml/.wxss/.json`。
- **语言**：面向用户文案用简体中文；注释、commit 用中文亦可，保持与现有代码一致。
- **样式**：优先复用移植自 `app.css` 的令牌与组件类，避免散落魔法数字与临时颜色。
- **组件化**：原型中重复出现的块（交易行 `.txn`、图表卡 `.chart-card`、设置项 `.li`、确认卡 `.confirm-card` 等）应抽成自定义组件复用。
- **不在前端硬编码敏感逻辑或身份**：权限、汇率固化、AI 数据范围都由云函数负责。
- **乐观更新优先**（判定标准见 PRD「三之二、交互响应原则」）：写操作默认「本地先改 → 后台同步 → 失败回滚 + 提示」，仅 PRD 列出的四类例外（服务端计算、待发标识、破坏性/导航流程、整页口径切换）用加载态。跨页交接用 `getApp().globalData`（现有先例：`justSaved` / `justDeleted`）；同页回滚先暂存 prev 再 setData。**新增或修改任何写操作时先按此判定，不等用户提。**

---

## 九、开发流程

1. **配环境**：在 `miniprogram/app.js` 的 `globalData.env` 填入云开发环境 ID（当前为空，未配置时云调用会报错）。
2. **起页面**：按第五节屏↔页对应，从大布局区到控件逐步移植；令牌先行。
3. **写云函数**：按第六节 type 分发 + 服务端鉴权；本地改完上传部署（含云端装依赖）。
4. **对验收**：每个功能对照 PRD 对应章节的「验收标准」自检。
5. **对像素**：UI 完成后与 `design/` 对应原型比对，确保视觉与交互一致、无横向溢出。

---

## 十、分期（Scope）

- **P1（MVP）**：账本(两类型)、成员与四级权限、记账(收支/两级分类/多币种固化/图片/记录人付款人)、图表(四默认卡+增删拖拽/每人一套)、AI(问答/收据/一句话，均预填确认)、导入导出、设置。分账结算型仅「能建+能记」。
- **P2**：分账自动结算（算谁欠谁 + 标记结清）、成员维度统计增强。
- **P3+**：图表库扩展、PDF 报表增强、批量导入向导、（远期）高级自定义 AI 模型。

新想法先进 PRD「待定问题清单」，不随意改动已冻结范围。
</content>
</invoke>
