# 心数 / Sense · 假数据与后端接口梳理

> 目的：盘点当前小程序里所有 mock（假）数据的使用位置、每个页面需要的数据、以及应对应的后端（云函数）接口。
> **本文档只做梳理，不含后端实现代码。** 后端遵循 [CLAUDE.md](CLAUDE.md) 的云函数「单入口 + `event.type` 分发 + 服务端 OPENID 鉴权」约定。

---

## 〇、假数据现在放在哪

假数据分两类来源，接后端时都要替换：

| 来源 | 位置 | 说明 |
|---|---|---|
| **集中 mock 模块** | [miniprogram/utils/mock.js](miniprogram/utils/mock.js) | 导出 `MEMBERS / DISPLAY_CURRENCY / BOOKS / HOME_SUMMARY / TXN_GROUPS / CATEGORIES / CURRENCIES / DETAIL / AI_MESSAGES / SETTLE / AVATAR_COLORS` |
| **页面内写死的数据** | 各 `pages/*/*.js` 的 `data` 与部分 `.wxml` | add 的成员/币种/分摊、stats 的图表数值、books 的账本/成员、settings 的个人信息等 |

> ⚠️ 重要：mock 里很多字段是**已格式化好的展示字符串**（如 `balance: '+¥8,642.20'`、`amount: '-¥86.00'`）。真实后端应返回**原始数值 + 币种**，由前端统一格式化。接口设计以「返回结构化数值」为准，本文档在相关处标注 🅕（需前端格式化）。

---

## 一、按页面盘点

### 1. home 首页 · [pages/home/home.js](miniprogram/pages/home/home.js)
**用到的假数据**：`mock.HOME_SUMMARY`、`mock.TXN_GROUPS`；`data` 内写死 `bookName='家庭日常'`、`displayCurrency='展示 · ¥ CNY'`。

**页面需要的数据**：
- 当前账本（名称、id）、当前展示币种。
- 本月概要：收入合计、支出合计、结余（按展示币种）🅕。
- 交易列表：按天分组、时间倒序；每条含 分类图标、标题、记录人/付款人摘要、金额、原始币种标识(fx)、当日汇率说明(sub)、收/支。

**对应接口**：
- `book.getCurrent` → 当前/默认账本 + 我的展示币种。
- `stats.getMonthlySummary(bookId, month, currency)` → { income, expense, balance }（数值）。
- `record.list(bookId, { currency, page, pageSize })` → 交易数组（后端可直接返回按天分组，或前端分组）。

---

### 2. add 记账录入 · [pages/add/add.js](miniprogram/pages/add/add.js)
**用到的假数据**：`mock.CATEGORIES`（一级+二级）；`data` 内写死 `CURS`（币种+汇率）、`members`（记录人/付款人）、`splitMembers`、`splitModes`、`dateLabel`、`note` 默认值。

**页面需要的数据**：
- 账本分类（一级九宫格 + 二级 chips，收/支各一套）。
- 币种列表 + **记账当日汇率**（用于实时换算预览与固化）。
- 账本成员（记录人 / 付款人 / 分摊成员）。
- 保存一笔记录（含固化汇率、分摊信息）。

**对应接口**：
- `category.list(bookId, type)` → 分类树。
- `rate.getDaily(date, baseCurrency)` → { CNY:1, EUR:7.83, ... }（当日或最近一次，附标注）。
- `member.list(bookId)` → 成员数组。
- `record.create(bookId, payload)` → 新记录；payload 含 type/amount/currency/rate/category/date/note/images/recorderId/payerId/split。
- （编辑场景）`record.update(recordId, payload)`。

---

### 3. detail 记录详情 · [pages/detail/detail.js](miniprogram/pages/detail/detail.js)
**用到的假数据**：`mock.DETAIL`（单条完整记录：原始金额、当日汇率、换算金额、分类、日期、记录人、付款人、备注、图片、`canEdit` 权限）。

**页面需要的数据**：
- 单条记录全部字段（含原始币种金额 + 已固化换算金额）。
- 当前用户对该记录的操作权限（能否编辑/删除）。

**对应接口**：
- `record.get(recordId)` → 记录详情（含 recorder/payer、原始与换算金额、图片 fileID）。
- `record.delete(recordId)` → 删除（服务端校验：本人或 admin/owner）。
- 权限由后端计算并随记录返回（`canEdit`/`canDelete`），前端不自行判断。

---

### 4. stats 统计 · [pages/stats/stats.js](miniprogram/pages/stats/stats.js)
**用到的假数据**：文件内写死 `CARD_DEFS`（四张卡片：本月概览数字、支出趋势折线点、近一年收支柱状、账本累计）；图表用内联 SVG。**布局**当前存在 `wx.storage`（key `sense.stats.layout.home.yu`）。

**页面需要的数据**：
- 各图表的统计数据（均**只统计当前账本**、按展示币种）：
  - 本月概览：收入/支出/结余。
  - 支出趋势：近 7 日按日的支出序列。
  - 近一年收支：按月的收入 vs 支出序列。
  - 账本累计：总收入/总支出/累计结余。
- 图表卡片布局（有哪些卡、顺序）—— **每「账本 + 用户」各一套**。

**对应接口**：
- `stats.getDashboard(bookId, currency)` → 一次性返回四类卡片数据（推荐），或按 `cardType` 分别取。
- `layout.get(bookId)` → 该用户在该账本的卡片顺序/可见集合。
- `layout.save(bookId, order)` → 保存布局（替换现有 wx.storage 方案）。

---

### 5. ai AI 助手 · [pages/ai/ai.js](miniprogram/pages/ai/ai.js)
**用到的假数据**：`mock.AI_MESSAGES`（会话历史 + 预填确认卡）；文件内 `parse()` 是本地关键词假解析。

**页面需要的数据**：
- 会话历史（**按账本**保留，上限默认 50，超出滚动删除）。
- 三种能力，均**返回预填记录、用户确认后才入账**（AI 绝不直接写库）：
  1. 数据问答（基于账本真实数据，不编造数字）。
  2. 收据识别 → 预填记录。
  3. 自然语言记账 → 预填记录。

**对应接口**：
- `ai.listMessages(bookId)` → 历史会话。
- `ai.ask(bookId, text)` → 问答回答（后端用账本真实数据）。
- `ai.parseText(bookId, text)` → 预填记录（自然语言）。
- `ai.parseReceipt(bookId, fileID)` → 预填记录（收据 OCR）。
- 确认入账复用 `record.create`；写会话用 `ai.appendMessage` + 服务端做 50 条滚动裁剪。
- 只读成员：可用 `ai.ask`，但不产生入账。

---

### 6. books 账本管理 · [pages/books/books.js](miniprogram/pages/books/books.js) + [books.wxml](miniprogram/pages/books/books.wxml)
**用到的假数据**：**大量写死在 books.wxml**——账本列表（家庭日常 / 日本旅行 2026）、成员（小雨 Owner / 阿哲 Admin）、角色徽标、账本类型；`books.js` 里 `roleLabel` 等为本地交互态。

**页面需要的数据**：
- 我的账本列表（名称、类型、我的角色、是否当前/默认）。
- 当前账本成员列表（头像、名称、角色、加入时间）。
- 各类管理操作。

**对应接口**：
- `book.list()` → 我加入的账本（含 myRole、isDefault）。
- `member.list(bookId)` → 成员。
- `member.invite(bookId)` → 生成微信邀请（分享）。
- `member.updateRole(bookId, memberId, role)` → 改权限（owner/admin 权限校验）。
- `member.remove(bookId, memberId)`。
- `book.update(bookId, { name, ... })` → 编辑账本信息。
- `book.dissolve(bookId)` → 解散（仅 owner）。
- `book.setDefault(bookId)`；新建账本跳 onboarding（见下）。

---

### 7. settings 我的/设置 · [pages/settings/settings.js](miniprogram/pages/settings/settings.js) + [settings.wxml](miniprogram/pages/settings/settings.wxml)
**用到的假数据**：`settings.wxml` 写死个人信息（小雨、已加入 2 个账本、默认账本）、AI 上限 50、版本；`settings.js` 内写死 `CURS`（展示币种循环）。

**页面需要的数据**：
- 用户信息（昵称、头像、加入账本数、默认账本）。
- 展示币种（读 + 改）。
- AI 会话保留上限（读 + 改，默认 50）。
- 导出 / 导入入口结果。

**对应接口**：
- `user.getProfile()` → 昵称/头像/账本数/默认账本。
- `settings.get()` / `settings.update({ displayCurrency, aiMessageLimit })`。
- `data.export(bookId, { format, range })` → JSON / Excel(含 CSV) / PDF。
- `data.import(bookId, fileID)` → 返回 { success, failed, reasons }（PDF 拒绝导入）。

---

### 8. onboarding 建账本 · [pages/onboarding/onboarding.js](miniprogram/pages/onboarding/onboarding.js)
**用到的假数据**：`data` 内写死 `curs`、`type`、`bookName` 默认值。

**页面需要的数据**：创建账本（名称、类型 共享可见/分账结算、基准币种）。

**对应接口**：
- `book.create({ name, type, baseCurrency })` → 新账本，创建者成为 owner，设为默认。

---

### 9. settle 分账结算（P2）· [pages/settle/settle.js](miniprogram/pages/settle/settle.js)
**用到的假数据**：`mock.SETTLE`（概览、转账方案 transfers、成员维度 members、分摊明细 splits）。

**页面需要的数据**：
- 结算计算结果：谁应收/应付、最少转账方案、成员垫付/应摊/净额、分摊明细。
- 标记某笔转账结清。

**对应接口（P2）**：
- `settle.get(bookId)` → 结算方案 + 成员维度 + 分摊明细（后端根据每笔付款人/分摊自动计算）。
- `settle.markTransfer(bookId, transferId, settled)`。

---

## 二、后端接口汇总（按云函数领域分组）

> 建议按领域拆多个云函数，每个内部仍用 `event.type` 分发。所有写操作在服务端用 `OPENID` 校验角色与可见范围。

| 云函数 | type（接口） | 主要入参 | 返回 | 权限/说明 |
|---|---|---|---|---|
| **book** | `list` | — | 我加入的账本[] | 仅返回我可见的 |
| | `getCurrent` | — | 当前/默认账本 + 我的展示币种 | |
| | `create` | name, type, baseCurrency | 新账本 | 创建者=owner，设默认 |
| | `update` | bookId, {name...} | ok | owner/admin |
| | `setDefault` | bookId | ok | |
| | `dissolve` | bookId | ok | 仅 owner，二次确认 |
| **member** | `list` | bookId | 成员[] | 成员可见 |
| | `invite` | bookId | 邀请信息/分享参数 | owner/admin |
| | `updateRole` | bookId, memberId, role | ok | owner/admin，规则见 PRD 权限表 |
| | `remove` | bookId, memberId | ok | owner/admin |
| **category** | `list` | bookId, type(收/支) | 两级分类树 | 账本级 |
| | `create`/`update`/`disable` | bookId, ... | ok | owner/admin；删除建议停用 |
| **record** | `list` | bookId, {currency,page,pageSize,dateRange} | 记录[]（可按天分组） | 按可见范围 |
| | `get` | recordId | 记录详情 + canEdit/canDelete | |
| | `create` | bookId, payload | 新记录 | 读写以上；含固化汇率 |
| | `update` | recordId, payload | ok | 本人 or admin/owner |
| | `delete` | recordId | ok | 本人 or admin/owner |
| **rate** | `getDaily` | date, baseCurrency | { 币种: 汇率 } + 是否「最近一次」标注 | 公开每日汇率 |
| **stats** | `getMonthlySummary` | bookId, month, currency | {income,expense,balance} | 仅当前账本 |
| | `getDashboard` | bookId, currency | 四类卡片数据 | 仅当前账本 |
| **layout** | `get` | bookId | 卡片顺序/可见集合 | 每用户×每账本 |
| | `save` | bookId, order | ok | |
| **ai** | `listMessages` | bookId | 会话[]（≤50） | 每账本 |
| | `ask` | bookId, text | 回答（基于真实数据） | 只读成员可用 |
| | `parseText` | bookId, text | 预填记录 | 不直接入账 |
| | `parseReceipt` | bookId, fileID | 预填记录 | 不直接入账 |
| | `appendMessage` | bookId, message | ok | 服务端滚动裁剪至上限 |
| **settings** | `get` | — | {displayCurrency, aiMessageLimit...} | |
| | `update` | {displayCurrency, aiMessageLimit} | ok | |
| **user** | `getProfile` | — | 昵称/头像/账本数/默认账本 | |
| **data** | `export` | bookId, {format,range} | 文件 fileID/下载 | JSON/Excel/PDF |
| | `import` | bookId, fileID | {success,failed,reasons} | PDF 拒绝 |
| **settle**（P2） | `get` | bookId | 结算方案+成员维度+明细 | 分账结算型账本 |
| | `markTransfer` | bookId, transferId, settled | ok | |

---

## 三、建议的云数据库集合（供接口落地时参考）

| 集合 | 关键字段 | 说明 |
|---|---|---|
| `books` | _id, name, type(share/split), baseCurrency, ownerOpenid, createdAt | 账本 |
| `members` | _id, bookId, openid, role(owner/admin/rw/ro), joinedAt | 账本成员关系（决定可见性与权限） |
| `categories` | _id, bookId, kind(income/expense), level, parentId, name, order, disabled | 账本级两级分类 |
| `records` | _id, bookId, type, amount, currency, rate, amountConverted(固化), categoryId, date, note, images[], recorderOpenid, payerOpenid, split{mode,members[]} | 记账记录 |
| `rates` | date, base, quotes{CNY,EUR,...} | 每日汇率快照 |
| `chartLayouts` | _id, bookId, openid, order[] | 每用户每账本图表布局 |
| `aiMessages` | _id, bookId, openid, role, content, createdAt | AI 会话（按账本裁剪 50） |
| `userSettings` | openid, displayCurrency, aiMessageLimit | 个人偏好 |

---

## 四、接后端时的注意点（呼应 PRD / CLAUDE.md）

1. **金额与币种**：mock 存的是格式化字符串，真实接口返回「原始数值 + 币种 + 固化换算值」，格式化放前端。
2. **汇率固化**：`record.create` 时把当日汇率与换算结果一并写库，之后不随汇率变动（历史稳定）。
3. **权限服务端强制**：`canEdit/canDelete`、成员管理、解散等由云函数按 `OPENID` 判定，前端不可信。
4. **可见性**：非成员完全查不到账本数据，须在查询层按 `members` 关系过滤。
5. **AI 不自动入账**：`parseText/parseReceipt` 只产出预填，确认后走 `record.create`。
6. **图表/展示币种**：统计只算当前账本；布局每用户每账本各一套；展示币种切换只改展示口径。
7. **分账结算**：P1 只「能建能记」，`settle.*` 属 P2。
