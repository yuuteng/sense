# 待你决定 / 待你操作

> 这份文件是你和我之间的**唯一待办清单**，不会被 agent 消息冲走。
> 我每次有新的待你决定项就更新这里；你回一句「1 跑、2 不跑」之类即可。
> 最后更新：2026-07-30 18:40
>
> **本轮所有代码工作已完成并验收通过。** 剩下的就是这份文件里 A1/A2/A3 三个决定 + B1/B2/B3 三项人工测试。

---

## A. 等你拍板（剩 1 项）

### ~~A1. 币种迁移~~ ✅ 已决定：**跳过**（2026-07-30 用户拍板）
理由：账本目前全是测试数据，迁移的唯一目的（保持现有账本显示口径不变）没有意义。
**已实测确认真正关心的那点**：新建账本不再被遮蔽，三种基准币逐一验过 —— base=EUR→首页 `€`、ISK→`kr`、JPY→`¥`，服务端解析与前端渲染一致，无需手动改顶栏胶囊。旧的 `settings.displayCurrency` 字段仍在用户文档里但已惰性（新代码不读），实测证据：若它还生效，三个新账本都会显示 `kr`。

### ~~A2. 重固化脚本~~ ✅ 已决定：**跳过**（2026-07-30 用户拍板）
理由：dry-run 显示只有 4 条记录需重算，**全部是演示账本的假数据**，最大变化 0.0043，冰岛零条。
**代码侧已修并部署**（`seedData.js` 两处 + `dataio.js` 一处 `round2`→`round6`；记一笔/编辑两条主路径 2026-07-22 就已是 round6），所以**以后新记的账不会再有这个问题**，A2 只管历史数据。
保留 `seed.refixAmountConverted`（dryRun 默认 true）备用，将来若发现精度类症状可随时跑。


### A1. 币种迁移要不要执行 ⏳
**背景**：删掉设置页「默认展示币种」后，没单独设过币种的账本会回落到自己的基准币。
**不迁移会变的只有 3 处**（dry-run 实测，不是推测）：

| 谁 | 账本 | 现在 | 不迁移就变成 |
|---|---|---|---|
| 你 | 家庭演示账本 | ISK | CNY |
| 你 | 旅行分账演示 | ISK | CNY |
| **运气超级好** | **冰岛** | **CNY** | **EUR** |

冰岛（你自己看）和「哈哈」都有自己的覆盖值，不受影响。
**第三行要留意**：那是冰岛的另一个真实成员，TA 不知道我们在改东西。
**我的建议**：跑。代价是零（只写 3 个覆盖值），好处是那位成员不会莫名看到币种变了。唯一副作用：TA 从此有个自己没设过的显式覆盖值，冰岛以后改基准币 TA 不会自动跟随。
**执行方式**：我把 `seed.migrateDisplayCurrency` 的 `dryRun` 改成 false 跑一次。

### A2. 重固化脚本要不要执行 ⏳ ← dry-run 已出，价值比预期小
**dry-run 实测（18:27Z）**：扫 126 条记录，**只有 4 条会变，全部是演示数据（`seed: true`，家庭演示账本），最大变化 0.0043。冰岛零条。**

| 记录 | 币种 | 旧值 | 新值 | 差 |
|---|---|---|---|---|
| 衣服 2025-12-07 | USD | 401.92 | 401.9157 | 0.0043 |
| 衣服 2026-06-30 | USD | 361.57 | 361.566227 | 0.0038 |
| 咖啡 2026-06-30 | EUR | 12.2 | 12.197016 | 0.0030 |
| 数码配件 2026-06-21 | JPY | 98.34 | 98.3401 | 0.0001 |

**所以它不碰你的真实数据**，只是把演示数据的固化精度对齐。**我原先说「一次修好四个页面」是高估了** —— 冰岛没有 2 位固化的存量记录，它那个「总额 1047.96 vs 明细 1047.97」纯粹是逐行取整的累加接缝，跟固化精度无关，真正解决它的是改总额定义（已部署，实测两边都是 1047.97 了）。
**我的建议**：跑，但优先级低。纯粹是让演示数据干净，零风险。你不跑也没影响。

### ~~A3. PRD 落笔~~ ✅ 已完成（2026-07-30 用户拍板后落笔）
改了 5 处，改前逐处核对过实际文本（不靠行号）：
- **4.3 规则**：删掉「设置页设全局默认，作为新账本的初始口径」，改为「唯一设置入口是账本顶栏币种胶囊（首页/统计/AI/结算均可点）；未单独设置过的账本，展示币种即该账本的基准币种」
- **4.3 验收标准**：新增两条 —— ①分账结算页金额与首页/统计同口径且不随汇率变动 ②新建账本未改胶囊时按基准币显示
- **4.8 包含项**：整条删除「默认展示币种设置」
- **4.8 验收标准**：改为「顶栏胶囊修改即时生效；未单独设置过的账本按基准币显示；**设置页不提供展示币种入口**」
- **5.8 区块与操作**：删去「默认展示币种设置」，加括注指向 4.3
- **待定问题清单第 5 条**：追加 2026-07-30 拍板记录

**残留检查**：全文 grep「全局默认 / 默认展示币种 / 新账本初始口径」，只剩待定清单里那处**故意保留的决策记录**。`Learning.md` 里的几处是历史复盘，应保留。
**顺带修掉一处文档自相矛盾**：`TODO.md` 第 101 行还标着「新建账本币种反直觉」未做，而 108 行说已随本轮解决 —— 已合并为一条并标注采用了当初列的候选解②。

---

## B. 等你人工操作（3 项，自动化确实做不到）

- [ ] **B1. 带图片时连点保存** —— automator 附不了真实图片，而图片上传耗时更长、幂等窗口更宽。跨两轮未覆盖。
- [ ] **B2. 那 4 个「未保存离开」确认框** —— 原生弹窗 automator 点不了。QA 验的是守卫状态和 API 调用次数，**框本身弹不弹、文案对不对没验过**。四个场景：①填了金额后返回 → 应弹「这笔账还没保存，确定离开？」②填了又清空后返回 → 不该弹 ③只加图片没填金额后返回 → 应弹 ④保存成功后返回 → 不该弹。
- [ ] **B3. 真机走一遍记账** —— 模拟器之外的最终确认。

---

## C. 已完成（不用你管，仅备查）

**Bug 修复 6 项，全部复测通过并部署**
1. records 页金额用错币种（差 18.5 倍，静默无报错）
2. 连点保存重复入账
3. 保存后 600ms 内返回会被多弹一层
4. 卡住时无任何提示
5. `syncUnloadGuard` 去重从未生效
6. 设置页说明文案（后被 A1 那批删除）

**产品变更 2 项**
- 删掉设置页「默认展示币种」入口 —— 已部署，9 项验收通过
- settle 改按记账当日汇率固化 —— 已部署，「不漂移」核心已验证（灵敏度对照：若仍用最新汇率，总额会是 85651.5 而非 1047.96，差 81 倍）

**过程中发现并修掉的额外问题**
- `settle.get` 死循环导致云函数超时（我们自己那批改动引入，QA 抓到）
- `fetchBookRecords` 无分页，超 1000 条记录的账本结算结果**静默错误**
- 净额取整不守恒 → 改用最大余额法（待部署）
- 开发者工具实例堆积会楔死模拟器导航层（27 个进程，手点也会卡）

**顺带解决的**
- 云函数现在能命令行部署，不用在 IDE 里右键上传（devlog 记过「改了没重部署，线上跑旧代码」的事故）
- 新建 EUR 账本不再显示 CNY（随 A1 那批顺带修好）

---

## D. 已记入 TODO.md 但本轮不做

见 `TODO.md` 第四、五节。要紧的两条：
- **币种小数位取整**：全站没有「某币种有几位小数」的元数据，ISK/日元/韩元会显示 `kr 1,431.74` 这种该币种不存在的小数。要改得动整个展示层。
- **`dissolveBook` 未清理孤儿键**：每次解散账本都会在用户文档里累积一个永不命中的键。
