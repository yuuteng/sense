# Learning.md · 教训沉淀

> 记录 **bug 复盘、踩坑、错误用法、反直觉结论、优化经验**。只记教训,不记进度(进度归 `.claude/devlog.md`)。
> 新条目加在最上面。格式:`## 日期 · 一句话标题`,正文写 现象 / 根因 / 教训(正确做法)。修完 bug 或发现坑时立即补记。

---

## 2026-07-30 · 「同一个量有两条计算路径」是多币种+取整场景的常驻风险:识别法 + 三个实例 + 接缝该放哪

> 与下一条(最大余额法)同源,合并成完整的一条。**识别方法:问「这个数字有没有第二种算法能算出来?如果用户会拿那两个数字比,它们必须共用同一条路径。」**

- 三个已确认实例(全部在 2026-07-30 一天内暴露):
  1. **成员净额 vs 转账行之和**:净额 `Σ round2(net_o)` 与转账金额各自取整 → 同屏「你应收 454.13」而两行加起来 454.14。**解法:在「分」空间用最大余额法统一离散化,两者都从同一套整数分派生。**
  2. **总额 vs 明细行之和**:`totalExpense = disp(totalBase)`(未取整总量的取整)与用户把 `splits[]` 逐行相加的结果差 0.01~0.02。**解法:把总额**定义**成明细之和** —— 用户验证总额的唯一手段就是把明细加起来,那就让它满足那个检查;`disp(totalBase)` 满足的性质没有任何用户能观察到。
  3. **逐笔短路值 vs 日聚合×系数**:`recToDisplay` 在「原币==展示币」时直接返回原金额(短路,零误差),而聚合路径走 `amountConverted × 系数`。**解法:提高中间固化精度让两条路径收敛到同一分**(round2→round6),而不是改口径 —— 改数据一次修好四个页面,改口径要动分层。
- 两个实现要点:
  ① **中间固化值的精度余量要按「最恶劣放大倍数」估,不是按「多两位小数」拍**。放大倍数 = 1/最小面值汇率:CNY→ISK 约 18 倍、CNY→KRW 约 **270** 倍(若基准币是 EUR,EUR→ISK 约 159 倍)。`round2` 误差上限 0.005 × 270 ≈ **1.35** —— 早就跨分;`round6` 是 5e-7 × 270 ≈ 1.4e-4,离 0.005 还有 ~35 倍余量,必然同分。**先算放大倍数,再定小数位。**
  ② **同一功能的多条实现路径必然精度漂移,改精度类问题必须按「所有写入口」grep 清点**。本例 `round6` 在 `record.create/update` 落地了整整一轮,而 `seedData`(2 处) 与 `dataio`(1 处) 全漏 —— 连「导入」这一个功能内部都有两条路径两种精度(`handlers.js:1861` 是 round6、`dataio.js` 是 round2)。判据:`grep 字段名: ` 找全部写入点,而不是只看主流程。
- 一条数学事实值得单独记住:**`Σ round2(x) ≠ round2(Σ x)` 恒成立,接缝无法消除,只能选它放在哪里**。原则是放在**用户最不可能做那个加法**的地方,优先级:「明细之和 vs 总额」必须消除(用户一定会加) > 「成员表某列之和 vs 总额」可接受(很少有人竖着加) > 「跨页跨周期对账」最低(本非等值关系)。选位置,不要幻想消除。

## 2026-07-30 · 「零和在全精度下成立」推不出「显示层零和成立」:round2 非线性,Σround2(x) ≠ round2(Σx)

- 现象:分账结算页同一屏自相矛盾 —— 成员净额 `小雨 -369.87 / 阿哲 -84.27 / 我 +454.13`(Σ = **-0.01**),而转账行 `369.87 + 84.27 = 454.14` ≠ 我的显示净额 `454.13`。「你应收 454.13」但两行加起来 454.14。
- 根因:真值 `-369.8667 / -84.2667 / +454.1333` 的全精度零和精确成立(2.27e-13),于是就以为显示层也零和。但 **`round2` 是非线性的,`Σ round2(x) ≠ round2(Σ x)`**。各成员独立取整后和不再为 0,而「成员净额」和「转账行金额」是**两条互不相干的取整路径**,各自取整必然对不上。
- 教训:①**凡「一个总量拆给多个主体分别显示」的场景,都必须用零和守恒的分配取整(最大余额法),而不是各自独立取整** —— 分账净额、分类占比、百分比凑 100%、按人均摊,全是这一类。判据很好用:**页面上同时显示「各项」与「合计」,或同时显示「某人净额」与「他的转账行」,就存在两条取整路径,必须共用同一套离散化结果。**②余数分配要用「**截断** + 按余数大小分配」,不要用「各自 `Math.round` 再把残差塞给最大的一方」—— 后者会给**真实值恰为 0 的主体**凭空造出一条 0.01 条目(页面显示某人要转 0.01,而这人一分不欠);截断法天然免疫,因为真实值为 0 者余数也为 0,显式跳过即可。③**把贪心/配对类算法从浮点搬到整数最小单位(分),终止性就从「靠迭代上限兜住」升级成「可证明不可能死循环」** —— 亚分净额的主体离散化后恰为 0,被筛选自动排除,连金额阈值都不再需要,上一条 Learning 记的那个 `[0.001,0.005)` 夹缝区被彻底根除,而不是再兜一层。④**排序做「多级 tiebreak」时,浮点相等的那一级必须带容差**:本例三人余数数学上都恰为 2/3,但 ×100 后浮点噪声差出 ~1.8e-12,于是第一级永不判平、后两级(|金额| → id)形同虚设,那 1 分按浮点尾数而非业务规则分配 —— 多级排序里只要有一级是浮点量,不加容差就等于只有一级生效。⑤**别用「不显示小于阈值的项」来掩盖取整残差**:那 1 分欠款不显示 ⟹ 无法标记结清 ⟹ 净额永不归零 ⟹「已全部结清」状态不可达 —— 用可结清的 1 分行换一个不可达的终态是明确退步。

## 2026-07-30 · 取整后的值不能用来驱动循环推进:round2(min) 压成 0 → 贪心死循环 → 云函数 -504003

- 现象:分账结算页对「旅行分账演示」100% 打不开,`settle.get` 必然超时 `-504003`。触发条件是 3 人均摊一个不能被 3 整除的金额(680→226.6667);2 人且金额除得干净的账本完全正常,所以之前一直没暴露。
- 根因:最少转账的贪心用**取整后**的值做推进 —— `const amt = round2(Math.min(d[i].v, c[j].v))`,当 `min` 落在 `[0.001, 0.005)` 时 `round2` 把它压成 `0`,于是 `d[i].v -= 0; c[j].v -= 0` 两边都不减,两个 `v` 都还在 `< 0.001` 的推进阈值之上,`i`/`j` 谁都不前进,`while` 永不结束。**「扣减 → 归零 → 推进」这条终止性链条,被取整从中间截断了。**
  更值得记的是它**为什么现在才炸**:同一批改动去掉了净额的中间 `round2`(这个改动本身是对的,消掉了「每成员各自取整后 Σnet=±0.01、贪心吐 0.01 垃圾互欠行」的伪影)。改之前 net 恒是 0.01 的整数倍,任何正残差 ≥0.01,`amt` 恒 >0.001,循环必然推进 —— 脆弱性一直在,只是输入域恰好躲开了那个窗口。
- 教训:①**贪心/配对/分摊这类靠「扣减到零」推进的循环,推进量必须用全精度,取整只允许发生在输出**。全精度 `min` 扣完必然把一侧精确归零(浮点误差 ~1e-13),指针必进,轮数有上界;一旦用取整值扣减,推进就不再有数学保证。②**判定条件要和取整后的值对齐**:既然只展示 ≥0.01 的转账,`push` 就该判 `round2(amt) >= 0.01`(而不是 `amt > 0.001`),推进阈值也该抬到半分 `0.005` —— 否则会出现「既不产生转账行、又达不到推进条件」的夹缝区。③**这类循环必须有迭代上限**:逻辑错应该表现为「结果不对」(可读日志、可定位),而不是把云函数打满到超时 —— 后者排查成本和线上代价都高一个量级,而且症状(整页打不开)完全掩盖了病因(1 分钱残差)。④**去掉一处「多余」的取整前,要先查下游有谁依赖「值是取整后的整数倍」这个隐含前提** —— 本例上游去 round2 是净收益,但它把下游一个从未被触发的分支变成了必经之路;精度类改动的影响面不在同一个函数里。⑤复现用例要挑**除不尽**的金额与人数(3 人均摊、680/249.6),整数和 2 人均摊会系统性地躲开这类残差 bug。

## 2026-07-30 · 选缩放系数时要问「它会不会随今天变化」,而不只是「零和还成立吗」:结算页金额随汇率漂移

- 现象:用户投诉「7月1日的钱,7月30日结算的时候再看反而增长了」。结算页同一批已记账记录的金额随日子变动;统计页成员卡却不漂。
- 根因:`settle.get` 展示换算用 `latestCnyQuotes()`(**最新**汇率)对基准币净额整体缩放。代码注释还明示这是有意取舍,理由是「所有数字同一系数缩放 → 净额零和与最少转账方案自洽」——**这个理由本身没错,但它只论证了零和,完全没论证「历史不变」**。而两者可以同时满足:按金额加权的日期固化混合系数 `F = Σ(A_r × f_r) / Σ A_r`(f_r = 该笔记账当日的系数)同样是「单一系数」,零和与方案自洽照旧,但不含「今天」这个变量,还额外满足 `disp(ΣA) == Σ(A_r × f_r)`(总额与首页/统计精确一致)。
- 教训:①**选缩放系数时必须问「它会不会随今天变化」,而不只是「零和还成立吗」** —— 用户能容忍近似,不能容忍历史数字自己变;论证一个取舍时要把**所有**该保的不变量列出来逐条验,只验了一条就写进注释当定论,会把 bug 伪装成设计;②**定位口径类 bug 要沿数据流找「最后一次乘汇率的地方」,不要从聚合逻辑开始重写** —— 本例 `amountConverted` 早已固化在基准币,净额、转账方案、结清抵扣本来就不漂移,出问题的只有最后那一个系数,改动因此只有几十行;误判成「聚合管道错了」会变成功能级返工;③**「抽公共函数」要抽到口径出口,抽到中间层等于只统一了一半** —— `settle` 与 `getMemberData` 共用 `splitShares` 就以为同源(注释也这么写),实际分叉在其下游换算层,一个乘最新汇率一个乘当日汇率,共享的那层恰好不是出错的那层;④**全量取集合的 helper 必须分页,`limit(1000)` 不是上限而是静默截断的陷阱** —— `fetchBookRecords` 用 `.limit(1000)` 无分页,与同文件 `getMemberData` 的正确分页并存,超 1000 条的账本结算净额直接错且不报错;同类操作在同一文件里出现两种写法,就说明有一种是错的;⑤**「不变」类需求要先问「能不能造出会变的数据」** —— 线上只能写当天汇率快照,没有任何接口能造历史汇率差,于是「不漂移」根本无法验收(只能验「数字变了」,验不了「数字没变」)。必须先补测试注入接口,否则交付的是「没验过的修复」。

## 2026-07-30 · 三级回退链里中间那级被注册时写死具体值,就永久遮蔽了最后一级:全局默认 'CNY' 让「按基准币兜底」永不可达

- 现象:用户建 EUR 基准币账本,首页却显示 CNY,必须再手动改一次顶栏胶囊。设置页那个「默认展示币种」入口也一直被用户嫌与顶栏胶囊重复(最终拍板删掉)。
- 根因:`displayCurrencyOf` 的优先级是「账本覆盖 > 用户全局默认 > 账本基准币」,看着是三级兜底很稳妥;但 `ensureUser` 注册时把 `settings.displayCurrency` **写死成 `'CNY'`**,于是中间级**永远有值**,最后一级「按账本基准币兜底」在任何真实用户身上都不可达。表面是「多一层保险」,实际是「最后一层永远用不上」。
- 教训:①**回退链的默认值宁可留空让下一级兜底,不要预填一个"看起来合理"的具体值** —— 预填 `'CNY'` 时它看着无害,却把整条链从三级压成两级,而且是静默的(没有任何报错会提示你最后一级已死);②判断回退链是否真的分层有效,要问「中间级有没有可能为空」——只要注册/初始化时就被填满,后面的级数全是装饰;③三级以上的口径回退链本身就是坏味道:本次删掉中间级后链条变成「账本覆盖 > 基准币」两级,既消掉了用户嫌重复的入口冗余,也顺带修掉了这个遮蔽 bug,两个问题同一个根因;④**删除回退链某一级时必须配一次性物化迁移**(把删除前的生效值写成下一级的显式覆盖),否则用户会看到金额口径突然全变,第一反应是「删功能把数据删坏了」—— 用户要删的是**入口**,不是要改变**现有口径**,这两件事必须分开;⑤迁移只补空缺、且生效值==兜底值时不写,才能既保「所见不变」又不把口径冻死。

## 2026-07-30 · showLoading(mask) 不是防重复提交;成功路径的 600ms 收尾窗口才是真漏洞,in-flight 标志只能在 catch 复位

- 现象:记一笔连点「保存」入 2 笔。全站 4 个云函数写入口,`ai.js:277`/`onboarding.js:46`/`feedback-new.js:58` 都有 in-flight 标志,唯独最高频的 `add.js:save()` 没有。
- 根因:①`wx.showLoading({mask:true})` 是**渲染层软防护,不构成写操作的重复提交保护**——showLoading 本身异步,遮罩上屏前的点击照样透传;②更隐蔽的是**成功路径**:`hideLoading` → `showToast`(默认 `mask:false`) → `setTimeout(600)` → `navigateBack` 这个本项目反复出现的收尾模式,天然留出约 600ms **无遮罩可点击窗口**,不依赖任何时序竞态,真机上必然存在。弱网连点被遮罩吞掉、请求返回遮罩消失,落在这 600ms 里的那一下就是第二笔。
- 教训:①所有触发云函数写操作的按钮必须有 in-flight 标志,`mask` 不算;②**标志只在 catch 里复位,绝不能用 finally**——成功路径要一直锁到页面离开,finally 复位等于原封不动重开那 600ms 窗口,等于没修;③标志要放在所有 `return` 型前置校验**之后**、showLoading **之前**,否则校验失败会把标志永久留 true 锁死页面;④「靠自觉逐处加」不可靠(4 个入口漏了最高频那个),该升格为 CLAUDE.md 第八节硬性条款 + code review 自检项。未防护待复核入口:`pages/settings` 批量操作(:132/:164/:214/:314/:336/:352/:368)、`pages/export`(:138/:151,有 mask 无标志,重复触发会重复生成文件/重复发信)。

## 2026-07-30 · displayCurrency 同一根因第二次发生:写下的教训没有执行,真正该消灭的是「调用方传参决定金额口径」这个模式

- 现象:统计页卡片 2650.12(ISK)钻取进 records 页明细汇总 143.46(CNY),18.5 倍偏差,无任何 console 报错。
- 根因:`record.list` 是全站 9 个展示口径出口里唯一不调 `displayCurrencyOf` 的一处(`const display = p.currency || base`),前端 `records.js` 的 `list.displayCurrency || 'CNY'` 硬兜底又把错误币种原样采信,二次放大。且 `ensureUser` 把全局默认写死 `CNY`,优先级「账本覆盖 > 全局默认 > 基准币」使**任何 baseCurrency ≠ CNY 的账本、用户什么都没碰也会踩中**。
- 教训:①**本条 Learning.md:35-36 已经记过一次**(结算页漏接展示口径),记了却只修了当时那一处,没有回头把同类出口全查一遍——**写下的教训不执行等于没写**,复盘条目应当附带一次全站同类点扫描,而不是只修触发它的那个页面;②真正的根因不是「某处漏调 displayCurrencyOf」,而是**「允许调用方传参决定金额口径」这个模式本身**:只要 `p.currency` 还被采信,任何未来新增的调用方(导出预览、AI 钻取、分享页)都会重新掉进来,所以正解是服务端权威解析、彻底忽略入参,而不是给漏掉的调用方补传参数;③前端 `|| 'CNY'` 这类硬兜底是「下一次事故的伏笔」——服务端保证有值后它不可达,留着只会在服务端出问题时把错误变静默,该删;④静默的口径错误比抛错危险得多(用户无法自查),这类跨页数值一致性必须有常驻契约用例:「同一请求传 currency 与不传,返回值逐字相等」。

## 2026-07-21 · 头像隐形真根因:fileID 失效时 image 渲染空白,wx:if="{{src}}" 分支永不降级

- 现象:结算页成员头像隐形。最初归因「后端缺 initial/color」,查完数据链发现 membersMap 兜底完善(initial/color 永不为空),后端没洞。
- 根因:该成员 avatarFileID **有值但文件失效**(清数据/seed reset 后云存储悬空、或跨环境 fileID)。组件 `wx:if="{{src}}"` 为真走 image 分支,下载失败 image 渲染为空白,字母圈分支永远轮不到。
- 教训:①远程图 + 文字兜底的组件,必须挂 `binderror` 降级(failed 标记 + src 变化时重置),「有 src」≠「图能显示」;②诊断显示类 bug 先分清「数据没给」vs「数据给了但资源失效」——名字正常说明数据链通,该怀疑资源层;③fileID 类引用在数据清空/迁移后会悬空,所有消费点都要能容忍。

## 2026-07-22 · 身份类字段的兜底只能取自同一个人,借别人的字段补 = 张冠李戴

- 现象:分账行付款人「运气超级好」头像圈显示「左」(记录人的首字);有头像的付款人也显示成字母。
- 根因:云函数新字段(payerInitial/payerAvatar)未部署返回 undefined,前端回退链写成 `payerInitial || recorderInitial`——缺数据时借了另一个人的身份。
- 教训:①身份字段(头像/首字/颜色)的兜底必须派生自**同一实体**(`payerName.slice(0,1)`、默认灰),宁可占位也不能拿别人的;②前后端同发的新字段,前端要按「后端还是旧版」设计回退——部署时序不受控,undefined 是常态而非异常。

## 2026-07-22 · 受控 focus 不随键盘收起复位,再点输入框键盘不弹

- 现象:记一笔页自动聚焦金额输入框后,手动收起键盘,再点输入框键盘不出现。
- 根因:`focus="{{amountFocus}}"` 由代码设 true 弹键盘;用户收起键盘时 data 里仍是 true。再点输入框,setData 无变化,框架不重新拉起键盘——受控属性与原生键盘状态失同步。
- 教训:凡用受控 `focus` 属性的 input,**必须配 `bindblur` 把该状态复位 false**,让下一次 setData(true) 或原生点击能再次生效。自动聚焦(体验优化)与手动聚焦(基本功能)共存时,失同步毁掉的是基本功能。

## 2026-07-22 · 换算金额固化到「分」会在小面值币种上放大回来:1000 ISK 变 1000.78

- 现象:记 1000 ISK,列表显示 kr1,000.78。
- 根因:固化管道 原币 → 基准币 `round2`(EUR 分级,6.288→6.29)→ 展示币换回,舍入误差被 1/rate(≈159 倍)放大;展示币面值越小越明显。
- 教训:①中间固化值精度要高于展示精度——`amountConverted` 用 `round6` 存,展示出口才 `round2`;②展示币种 == 记录原始币种时直接用原始金额(recToDisplay 短路),彻底零误差;③**汇率按有效数字保精度,不按小数位**——ISK→EUR ≈ 0.0062882 被 round6 只剩 4 位有效数字,聚合口径(月汇总/图表)仍差 0.07,改 `toPrecision(12)` 后归零;金额才用固定小数位舍入;④涉及汇率往返的功能,用小面值币种(ISK/JPY/KRW)做用例,cent 级误差在这些币上肉眼可见。存量老记录:重新编辑保存一次即按新精度重固化。

## 2026-07-21 · 双币种口径(基准币固化/展示币)下,新页面漏接展示口径会被用户当 bug

- 现象:「ISK 账本」结算页全是 €。其实账本基准币是 EUR(建账时默认没改),用户把每账本展示币种设成 ISK,首页/统计都显示 kr,唯独结算页直出基准币——用户认知里这就是"币种错了且改不了"。
- 根因:结算页(后接的新页面)只用了 baseCurrency,没接 displayCurrencyOf 展示口径;全站口径不一致把「基准币 vs 展示币」的内部区分暴露给了用户。
- 教训:多币种双口径架构下,**任何新增页面的金额展示默认走展示币种口径**(displayCurrencyOf),基准币只做内部台账(欠款、抵扣、固化金额);服务端返回时区分 `amount`(内部币)与 `amountDisp`(展示币),回传写操作用内部币。新页面自检项:「这页金额和首页切换展示币种后一致吗?」

## 2026-07-21 · text 节点省略号真机不稳定:溢出文字会画到相邻元素上,截断一律用 view

- 现象:分账结算行(名字+箭头+名字+金额+按钮单行),`<text class="ellip" style="max-width:170rpx">` 在真机上不截断,长昵称直接叠画在金额 €5.00 上。
- 根因:text 节点是行内渲染,overflow/text-overflow 在部分真机不生效;溢出内容不裁剪,直接压过 flex 兄弟节点。且单行塞五个元素本身放不下,截断失效后必然重叠。
- 教训:①需要省略号截断的文字用 `view`(块级,overflow:hidden 稳定),别信 text+ellipsis;②行内元素超过 3 个(双名字+金额+按钮)就该改两行布局,不要指望截断救拥挤。

## 2026-07-21 · 组件对空入参无兜底 = 元素「隐形」:darken('') 返回空串,头像圆圈透明消失

- 现象:结算页某成员头像整个不显示(既无图也无字母圈),名字却正常。
- 根因:该成员后端资料缺 initial/color,avatar 组件 `darken('')` 对非法色原样返回空串 → background:'' 透明 + initial 空 → 圆圈存在但完全不可见,比报错更难发现。
- 教训:展示型组件对每个入参都要有可见兜底(空色给默认灰、空文字给「?」),「空值→透明/空白」是最阴险的降级,宁可显示占位符也不要隐形;调用侧(页面映射层)同样做一层 `|| 默认值`,双保险。

## 2026-07-21 · 汇率快照只存 base=CNY,却按请求 base 查库:非 CNY 基准账本全废

- 现象:账本基准币选 ISK/EUR,记一笔默认币种不对、切任意币种报「该币种暂无汇率」、拉取当日汇率后仍是 blocker。
- 根因:`fetchAndStoreCnyQuotes` 只写 `base=CNY` 快照,但 `rate.getDaily` 与 `lib.getRate` 都按 `where({base: 请求base})` 查库。非 CNY base 永远查不到 → getDaily 返回 `{quotes:{base:1}}`(前端 `CURRENCIES∩quotes` 只剩一项,切换全落空)、getRate 直接 throw RATE_UNAVAILABLE(存不了记录)。刷新只补 CNY 快照,查询路径没变,所以无解。
- 教训:枢轴币模型下,**所有读汇率的入口都必须按枢轴币(CNY)取快照再换算目标 base**,不能按目标 base 查库。base 每 1 currency = (CNY/currency)/(CNY/base)。存储口径和查询口径必须一致;加新基准币时不需要新快照,只要枢轴币快照里含该币即可。

## 2026-07-21 · 微信审核:进入即强制授权必被拒,openid 静默注册才是正解

- 现象:审核失败「打开一进入首页,未浏览体验功能服务,即要求授权手机号码、头像、昵称进行授权登录」。
- 根因:home 有登录闸门,未注册直接 reLaunch 登录页强制填昵称/头像。
- 教训:小程序身份 = openid(云函数 getWXContext 免授权自动拿),**根本不需要登录页**。正解:服务端 ensureUser 静默建用户 + 哈希生成默认昵称/头像色,所有功能直接可用;昵称头像改成「我的」里可选修改。审核红线:任何授权(头像/昵称/手机号)只能在用户主动操作时按需请求,不能做成进入门槛。

## 2026-07-21 · JS 无符号哈希做索引:>> 会把大数变负,必须用 >>>

- 现象:`h >>> 0` 保证无符号后,`(h >> 4) % len` 仍取出 undefined。
- 根因:`>>` 是有符号移位,h > 2^31 时先转 int32 变负,负数取模还是负,数组负索引 = undefined。
- 教训:哈希值做数组索引,移位一律 `>>>`;写完用批量随机输入跑一遍(这次 1000 个随机 openid 测试当场抓住)。

## 2026-07-10 · 「标记状态」挂在实时重算的衍生结果上必错位,持久化要挂到事实本身

- 现象:分账结算的「标记结清」只存了 transferId(t1/t2 序号),而转账方案每次进页全量重算——新记一笔账后方案整体重排,旧标记即使落库也对不上新方案;且标记≠还款事实,已转的钱下次重算照样算成欠款。
- 根因:把「状态」钉在衍生视图(计算出的方案)的临时坐标上,而不是钉在事实(谁已付给谁多少)上。
- 教训:对"实时计算 + 可标记"类需求,落库的应是**事实型抵扣**(from/to/amount 的 settlements 文档,重算时冲抵净额),不是视图行的勾选态。抵扣模型天然支持"结清一部分→继续记账→只算增量",不需要周期/快照;撤销 = 删抵扣文档。序号型 id(t1/t2)永远不要当持久化外键,要么内容寻址要么落成独立文档。

## 2026-07-10 · 多规则文本解析:模式间会互噬,词素重叠处必须加边界断言

- 现象:parseNL「先挖日期再抽金额」策略下,「地铁 220 日元」被解析成 amt=2:日期正则 /(\d{1,2})\s*[号日]/ 把「20 日」当成了日期(「日」既是日期单位又是「日元」首字),挖掉后金额只剩「2」,币种也丢了。
- 教训:①领域词素重叠(日/号/点/块)处,正则要加前后断言封边界((?<![\d.])、(?!元|币));②「先抽 A 再抽 B」的流水线里,A 的误匹配会级联毁掉 B——每加一条规则都要跑全量用例;③解析器改造先写用例表再动正则,这次 3 处 bug 全是用例抓的,肉眼一处没看出来。

## 2026-07-10 · 模拟器 watchdog 卡死后,清缓存+重编译救不回来,必须重启工具进程

- 现象:先弹「模拟器长时间没有响应」,之后小程序打开永远白屏(无导航/无 tabBar/无报错可见);清缓存、重新编译均无效。
- 根因:模拟器 webview 内核进入坏状态,缓存清理不重置 webview 进程。
- 教训:遇到 watchdog 弹窗或白屏,第一动作是**完全退出开发者工具进程**(任务管理器确认无残留)再重开,而不是在坏状态里反复清缓存/重编译排查代码;代码层排查(WXML 配平/JS 语法/Console)留到重启后仍复现时再做。

## 2026-07-10 · 悬挂指针型 bug:删实体不修指向它的引用,读端必须兜底

- 现象:解散默认账本后用户被直接弹去「重新建账本」,哪怕还有别的账本。
- 根因:dissolveBook 删了 books/members,但所有成员的 users.defaultBookId 仍指向已删账本;getCurrent 发现账本不存在就直接 return null,没有回退。写端(解散)也修不全——其他成员、被移出、主动退出同样产生悬挂指针。
- 教训:引用型字段(defaultBookId 这类)失效场景多在「别处删除」,写端逐一补救必有遗漏;正确做法是**读端解析时兜底**:校验目标有效→失效则回退到下一个合法值并把修正落库。一处兜底覆盖所有产生悬挂的路径。

## 2026-07-10 · 换色要按「管道」清点,CSS 变量到不了的边界全是漏点

- 现象:墨水层迁移自认全量完成,audit 复查发现 CSS 内 ~95% 干净,但漏了 5 类边界:JS 传入的图标色(icons.get)、wx.showModal 的 confirmColor(8 处旧红)、wxml 内联 style(2 处 var(--danger))、JS 常量(stats.js AXIS)、以及 5 个 input 根本没挂 placeholder-class(微信默认 #999 占位 ≈2.8:1)。
- 根因:修复按「页面/表面」推进,而颜色实际流经多条管道:WXSS 变量、内联 style、JS 参数、原生组件参数、组件默认值。只扫 WXSS 就只修了一条管道。
- 教训:全局换色/换 token 时按管道列清单逐条 grep:①wxss color/background ②wxml style= ③JS 色值字符串(含 showModal/showActionSheet/图标工厂)④组件 properties 默认值 ⑤placeholder-class 挂载情况。修完以「值」反查(旧 hex 全局 grep 应为 0),不以「文件」自查。

## 2026-07-09 · 设计评审的前提要对照代码核实,别照单全收

- 现象:全页面 critique 报「误触币种胶囊 = 整页口径切换」(P 级红旗)、「图表编辑只能长按发现」。核对代码:胶囊误触只弹选择面板,真正切换要在面板里再点币种(home.js onCurPick);统计页本就有可见的「编辑」链接(stats.wxml sec-actions)。两个前提都不成立,真问题其实是"黄胶囊视觉太抢"与"无标注 FAB"。
- 教训:LLM 评审(包括子代理)对交互后果的推断可能基于表面 UI 想象。据评审改代码前,先把涉及的事件链读一遍;修"想象出来的问题"会引入真回归。

## 2026-07-09 · sed 批量换色会误伤 JSON 的 canonical 值与 ramp

- 现象:对 design.json 跑 `sed s/#0089c0/#01749f/` 想只改组件 CSS 片段,结果把 lake-blue 自身的 canonical 和 tonalRamp 也改了,两个色板条目变成同一个值。
- 教训:结构化文件(JSON/YAML)里同一 hex 承担多重身份(定义 vs 引用),全文替换必误伤。要么按字段精确 Edit,要么替换后 diff 复查每处命中。

## 2026-07-09 · WCAG 大字判定:16px 加粗不算「大字」

- 现象:想当然把 32rpx(≈16px)/600 的按钮文字按「大字 3:1」豁免。查标准:大字 = ≥24px 常规或 ≥18.66px 加粗,16px bold 不够,仍需 4.5:1。
- 教训:「加粗就算大字」是常见误记;判定用 18.66px bold / 24px regular 两条硬线。白字压亮青(1.9:1)连 3:1 都不过,只能作为登记在案的品牌例外,不能靠"它是大字"糊弄。

## 2026-07-09 · 云函数 -504003 = 执行超时,循环 await 数据库调用是主因

- 现象:reset 数据后创建账本必报 `cloud.callFunction:fail errCode: -504003`,卡死在建账本页。
- 根因:云函数默认超时 3 秒。`book.create` 串行跑 11 次 `createCollection` + 68 条默认分类逐条 `add`,全新环境下必超。之前集合已存在、createCollection 秒失败,所以没暴露。
- 教训:云函数里任何 `for + await` 的数据库调用都是雷。独立调用用 `Promise.all` 并行;批量写用数组插入(`collection.add({ data: [/* 多条 */] })`,服务端 SDK 支持,返回 `_ids` 按插入顺序)。`config.json` 可设 `"timeout": 20` 兜底,但先修代码。

## 2026-07-09 · 原生 button 宽度/display 不可控,会撑坏 flex 行

- 现象:凡把 `<button>` 放进 flex 布局,设置 width 常被忽略,button 撑满剩余宽度,同行内容被挤变形(两次踩坑:bookConfig 邀请胶囊悬在中间;settings 头像行昵称被挤到右侧换行)。
- 教训:需要 `open-type` 能力(share/chooseAvatar)时,button 只做触发器,不承担视觉与布局。两个可靠模式:①button 自身当 flex 容器,视觉胶囊放内层 view(invite-btn);②视觉用普通 view 排版,button 绝对定位透明覆盖其上(avatar-choose)。

## 2026-07-09 · seed 数据用固定 _id = 多用户共享单例,互相覆盖

- 现象:A、B 各自「载入演示数据」后账本"被合并";B 加入 A 账本被判"已是成员";清除演示数据删掉对方的。
- 根因:演示文档 id 写死(`seed-book-share`),所有用户写的是数据库里同一条文档。
- 教训:任何用户可触发的固定 id 写入都是共享单例。多用户隔离 = id 带调用者后缀 + 每条文档记 `seedBy` 归属,清理只按归属删。

## 2026-07-09 · 微信分享必须用户直点 open-type=share,参数走 dataset

- 现象:想"点邀请→选权限→再分享",不能程序化拉起分享。
- 教训:分享只能由用户点击 `<button open-type="share">` 触发。需要带不同参数时,放多个 share button 各挂 `data-*`,在 `onShareAppMessage(res)` 里读 `res.target.dataset` 拼 path。

## 2026-07-09 · iOS 数字键盘的小数点可能是逗号/句号

- 现象:iOS 部分地区 `type="digit"` 键盘出 `,` 或 `。`,金额解析失败。
- 教训:金额输入统一归一化 `.replace(/[,，。]/g, '.')` 再解析。

## 2026-07-08 · canvas 同层渲染:开发者工具模拟器不可信,真机才是事实

- 现象:开发者工具里图表穿透 tabBar/弹层、或不渲染;真机完全正常。
- 根因:模拟器对 canvas 2d 同层渲染支持不完整;且**热重载会破坏同层渲染**(已在 project.private.config.json 关掉 `compileHotReLoad`)。
- 教训:canvas 层级/渲染问题先上真机验证,别在模拟器里空转排查代码。

## 2026-07-08 · 组件 properties observer 不会为初始值触发

- 现象:图表组件首次 setData 的 option 永远不渲染,只有后续变更才画。
- 根因:observer 只在属性**变更**时触发,初始值在 attached 前已就位,不算变更。
- 教训:组件 `ready()` 里必须 fallback 读 `this.data.xxx` 处理初始值,observer 只管后续更新。

## 2026-07-08 · setData / 属性传递会剥离函数

- 现象:把 echarts 实例经 property 传给 ec-canvas,组件里拿到的对象没有方法,报 `xxx is not a function`。
- 教训:跨组件传的数据都会被序列化,函数/类实例全丢。库实例在使用它的组件内部 `require`,不走属性。

## 2026-07-08 · ECharts 按需构建缺模块 = 静默空白,不报错

- 现象:折线图空白无任何报错。根因:自定义 bundle 没打进 LineChart,`series.line` 未注册时 ECharts 静默忽略。
- 教训:按需构建后必须验证:`grep 'series.line"' echarts.js` 确认用到的 series/component 已注册,再交付。

## 2026-07-08 · API 信封字段与业务字段共用命名空间会互相覆盖

- 现象:筛选传 `type:'income'` 覆盖了路由字段 `type`,报「未知接口 record.income」。
- 教训:`{...params, resource, type}` 让路由键 spread 在**最后**,payload 永远盖不掉;且业务字段避开信封字段名(改 `recordType`)。信封与业务字段从设计上分命名空间。

## 2026-07-08 · scroll-view 不滚 = 高度链断了;弹层要挡穿透

- 现象:半屏弹层里 scroll-view 滚不动,手势落到底层页面上。
- 教训:scroll-view 必须有**确定高度**——从固定高容器(如 `height: 78vh`)一路 `flex:1; height:0` 传下来,任一环节缺就不滚。遮罩层加 `catchtouchmove` 阻止滚动穿透。

## 2026-07-05 · color-mix()/oklab 低版本基础库不支持

- 现象:设计令牌里的 `color-mix()`/`oklab` 颜色在部分机型上失效。
- 教训:移植设计令牌时把 color-mix 结果**预换算成固定色值**写进 wxss,视觉结果逐字对齐原型。
