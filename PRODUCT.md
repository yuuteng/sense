# Product

## Register

product

## Platform

ios

> 实际载体：微信小程序（WXML/WXSS，rpx 响应式），运行于 iOS/Android 双端，但设计语言统一对齐 iOS（HIG 参照）：`design/` 原型即 iPhone 高保真，导航/开关/弹层/触感均按 iOS 习惯设计，不做 Material 分支。

## Users

- 有记账习惯或想开始记账的普通人；核心场景是手机上"随手记一笔"，单手、碎片时间、常在户外或消费现场。
- 有共同开支的家庭、情侣、室友、旅行/项目小团体——多人往同一账本记账，关心"谁记的、谁付的"。
- 常涉多币种的人群（出国、跨境购物、留学、外币收入）——要的是"混着记、统一看、历史不乱跳"。
- 角色分四级（owner / admin / 读写 / 只读），只读成员只看账和问 AI，不记账。

要完成的工作（job to be done）：以最少的点击把一笔账记下（3 步内），随时看清"这个月花了多少、花在哪"，多人协作时不为权限和口径扯皮。

## Product Purpose

心数 / Sense——简洁轻量、支持多人协作与多币种、内置 AI 助手的微信小程序记账应用。成功的样子：

- 记得轻松：3 步内完成最简记录；乐观更新，操作后界面 ≤100ms 变化。
- 算得清楚：多币种按记账当日汇率固化换算，历史金额永不漂移。
- 看得明白：开箱即用的 ECharts 图表卡片，可增删拖拽、每人一套布局、可点击钻取。
- 问得省心：AI 数据问答 / 收据识别 / 一句话记账，全部预填确认、绝不自动入账。

功能事实来源：`Sense-PRD.md`（P1 范围冻结）。UI 事实来源：`design/` 原型（只读）。

## Brand Personality

**轻盈 · 亲和 · 省心。**

- 轻盈：界面呼吸感足，一屏只做一件事；记账是几秒钟的小动作，不是一次"任务"。
- 亲和：文案说人话、不用财务黑话；错误与空状态给出路，不甩锅给用户。
- 省心：默认值替用户做好选择（今天、当前币种、常用分类靠前）；数字口径永远一致，用户不需要自己核对。

情绪目标：打开时平静、记完时轻松、看报表时踏实。不制造记账焦虑，不用红色大字吓唬用户超支。

## Anti-references

- **随手记式密集堆砌**：广告位、金融产品推销、功能入口塞满首页的传统记账 app。首页只有账本、概要、记录列表、记一笔——不加"发现""商城""借钱"。
- **营销页花哨感**：渐变大字、装饰性插画堆砌、campaign 式视觉。这是每天用的工具，不是落地页；视觉服务于数字的可读性。
- （辅助边界，来自 CLAUDE.md）不把原型的 iPhone 画框 chrome 搬进小程序；不改 `design/` 设计令牌的值。

## Design Principles

1. **数字第一可读**：金额是页面主角——等宽数字、tabular-nums、高对比；任何装饰不得抢数字的焦点。
2. **默认值即服务**：每个输入都先替用户填好最可能的值，用户只改例外。少一次点击胜过多一个功能。
3. **口径一处定义**：同一数字在列表、卡片、钻取明细里必须相等；换算/聚合逻辑只在服务端存在一份。
4. **零等待，有回路**：写操作乐观更新（≤100ms 反馈），失败回滚 + 明确提示；四类例外（服务端计算/依赖标识/破坏性/整页切换）用加载态（判定见 PRD 三之二）。
5. **AI 是助手不是代理**：AI 产出永远是"预填 + 确认"，绝不代替用户入账；答案基于账本真实数据，不编造。

## Accessibility & Inclusion

**严格 WCAG AA 全项**：

- 对比度：正文 ≥4.5:1，大字/加粗 ≥3:1；placeholder 同样 ≥4.5:1。文字一律用墨水层令牌（`--accent-ink` #01749f、`--success-strong` #4a7d0b、`--warn-ink` #8a690a、`--danger-ink` #c41e5a、`--muted` #5f6c7d，均对白底 ≥4.5:1），亮色只做填充。
- **已知品牌例外（明示登记，不得新增）**：
  1. 湖心青填充面上的白字/白图标（主按钮、FAB、选中分类图标，≈1.9:1）与 hero 渐变上的白色文字——依赖形状、位置与相邻墨水字冗余识别；
  2. ECharts 系列填充色（嫩芽绿 1.6:1 / 湖心青 1.9:1 对白卡，低于非文字内容 3:1）——图例文字、排行列表与 +/- 符号提供完整冗余，图形本身仅作辅助。
- 动效：所有动画提供 reduced-motion 降级（`app.wxss` 末尾媒体查询块；ECharts canvas 动画不受媒体查询管辖，属已知平台限制）。位移/缩放允许**轻微回弹**（`--ease-spring` overshoot 1.4，DESIGN.md 登记的「会呼吸」品牌手感）；禁止大幅 bounce/elastic 曲线。
- 图表：ECharts 系列色需色盲安全（不单靠红绿区分收/支，配合图例文字与位置）；收入/支出在色彩之外有文字/符号标识。
- 触控目标 ≥ 88rpx（44pt），iOS HIG 标准。
- 金额等关键数字不以颜色为唯一信息载体。
