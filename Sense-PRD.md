# 心数 / Sense 产品需求文档（PRD）

> 版本：v1（P1 范围冻结版）
> 文档性质：产品与功能层面描述，不含任何技术实现内容
> 说明：本文档同时作为产品 backlog——P1/P2/P3 全部功能已写全，完成一期勾一期，避免遗漏。

---

## 一、产品概述

**一句话定位**：一款简洁轻量、支持多人协作与多币种、并内置 AI 助手的记账小程序。

**目标用户**：有记账习惯或想开始记账的普通人；有共同开支需求的家庭、情侣、室友、旅行/项目小团体；经常涉及多种货币（出国、跨境购物、留学、外币收入）的人群。

**核心价值**：

1. **记得轻松**——记一笔尽量少点几下，支持备注、拍照、多币种混合录入。
2. **算得清楚**——多币种自动按当天汇率换算成一种货币展示，历史金额不再乱跳。
3. **看得明白**——开箱即用的收支图表，可自由增删、排序。
4. **问得省心**——AI 助手能回答"我上个月吃饭花了多少"，也能读收据帮你把账记好。
5. **能协作**——账本可邀请成员、区分权限、区分每笔是谁记的/谁付的。

---

## 二、功能范围与分期

> 范围一旦冻结即为该期交付目标；后续新增想法先进"待定问题清单"，不随意改动已冻结范围。

### P1（首个可上线版本，MVP）

- 账本：可创建两种类型（共享可见型 / 分账结算型）；默认账本、切换账本、修改默认账本。
- 成员与权限：owner / admin / 读写 / 只读 四级；邀请、踢人、改权限；未受邀不可见。
- 记账：收入 + 支出；两级分类（预设 + 自定义，账本级）；金额、备注、图片附件；区分记录人 / 付款人。
- 多币种：混合录入，按记账当日汇率固化换算；列表与图表按用户所选展示币种统一显示。
- 图表：统计当前账本；四个默认卡片 + 可添加「分类占比（含排行）」卡；月度类卡片可选任意历史月份；图表可点击钻取到记录筛选列表；卡片可增删、可长按拖拽排序；布局每人各自一套。渲染基于 ECharts（小程序定制包）。
- AI 助手：数据问答、上传收据自动识别并预填、自然语言记账（均先预填让用户确认再入账）；会话记录完整保留；免费用户 AI 调用 50 次额度（付费解锁后续接入）。
- 导入导出：导出 JSON / Excel（含 CSV）/ PDF；导入 JSON / Excel（含 CSV）；PDF 仅导出。
- 设置：展示币种、分类管理入口、导入导出入口、关于等。（AI 会话上限为后台默认值，不暴露给用户。）
- 用户反馈（轻量工单）：提交反馈（标题、内容、图片附件、选填联系邮箱）；「我的反馈」列表查看处理状态与客服回复线程；客服由管理员白名单在小程序内回复与改状态。

> 说明：**分账结算型账本在 P1 只做"能创建 + 能记录（谁付款、如何分摊）"，不含"自动算出谁欠谁并结清"的结算逻辑**，该逻辑在 P2 完成。

### P2

- 分账结算：根据每笔的付款人与分摊，自动计算成员之间的债权债务（谁欠谁多少），支持"标记结清"。
- 成员维度统计增强：按成员查看支出/分摊/结算情况的图表与列表。
- 用户反馈增强：客服回复后通过**微信订阅消息**通知用户；客服侧接入**云开发 CMS 网页后台**处理工单（摆脱在小程序里当客服）。（客服 owner/admin 两级与邀请机制已提前在 P1 实现，见 4.9。）

### P3+（增强与远期）

- 可配置图表库扩展：更多图表类型与维度（币种维度、成员维度联动、自定义起止区间等）。（注：分类占比与点击钻取已提前至 P1 实现，见 4.5。）
- PDF 报表增强：更完整的对账单/月报样式。
- 批量导入向导：从其他记账软件迁入时的"列映射 + 分类/币种映射 + 去重"引导。
- 客服体系升级：接入微信官方「微信客服」实时人工会话，工单系统退化为异步问题追踪；新反馈邮件通知客服邮箱（云函数 nodemailer，需独立发件邮箱授权码）。
- （远期）高级用户自定义 AI 模型。

---

## 三、角色与权限

权限**跟随账本**：同一个用户在不同账本里可以是不同角色。未被邀请进某账本的用户，完全无法看到或访问该账本。

| 能力 | Owner（所有者） | Admin（管理员） | 读写成员 | 只读成员 |
|---|---|---|---|---|
| 查看账本内容（记录、图表） | ✅ | ✅ | ✅ | ✅ |
| 新增记录 | ✅ | ✅ | ✅ | ❌ |
| 修改 / 删除**自己**记的记录 | ✅ | ✅ | ✅ | ❌ |
| 修改 / 删除**他人**记的记录 | ✅ | ✅ | ❌ | ❌ |
| 邀请新成员 | ✅ | ✅ | ❌ | ❌ |
| 移除成员 | ✅ | ✅（不能移除 owner/其他 admin） | ❌ | ❌ |
| 修改成员权限 | ✅ | ✅（不能改 owner；可在读写/只读间调整普通成员） | ❌ | ❌ |
| 任命 / 取消 admin | ✅ | ❌ | ❌ | ❌ |
| 新增 / 停用分类（记账时可随手增删） | ✅ | ✅ | ✅ | ❌ |
| 重命名 / 排序分类 | ✅ | ✅ | ❌ | ❌ |
| 编辑账本信息（名称等） | ✅ | ✅ | ❌ | ❌ |
| 解散 / 删除账本 | ✅ | ❌ | ❌ | ❌ |
| 退出账本 | —（owner 需先转让或解散） | ✅ | ✅ | ✅ |

补充规则：

- 每个账本**有且仅有一个 owner**，即创建者；owner 可将所有权转让给他人（P1 可先不做转让，列入待定）。
- **图表布局是每个用户自己的**，与权限无关，任何成员都可以自由摆放自己看到的图表，不影响别人。
- 各项统计、AI 问答的数据范围，均受当前账本与该用户可见范围约束。

---

## 三之二、交互响应原则（全局，适用于所有功能）

> 所有新增或修改的功能，设计每个写操作（新增 / 修改 / 删除 / 状态切换）时**必须先做本判定**，无需逐次另行评审。

**乐观更新优先**：先回答「能否本地立即呈现结果、后台同步、失败回滚」。能，则必须乐观更新——用户操作后界面**零等待**变化，云端同步失败时回滚并明确提示。

**四类例外**（不做乐观，改用明确的加载态）：

1. **结果需服务端计算才正确**——如金额按记账当日汇率换算、统计聚合、AI 生成。本地给不出真数字，宁可等待也不显示假数。
2. **后续操作依赖服务端生成的标识**——新建分类要拿 categoryId、提交反馈要拿工单号、生成邀请码等。
3. **破坏性或导航型流程**——解散账本、创建账本后进首页、加入账本。必须确认服务端成功后再继续。
4. **整页口径切换**——切换账本、切换展示币种的数值部分。全量数据更换，加载态即正确形态；但触发它的按钮/标签本身应立即反馈（如币种胶囊先变）。

**验收标准**：乐观类操作点击后界面变化 ≤ 100ms，失败有回滚与提示；例外类操作有明确加载反馈。评审新功能时对每个写操作标注「乐观 / 等待」及归类理由。

---

## 四、功能详述

> 每个功能包含：用途、操作流程、规则、验收标准。

### 4.1 账本管理

**用途**：把不同场景的账分开管理（如"家庭日常""日本旅行""合租房租"），并决定该账本是否需要成员间结算。

**操作流程**：

1. 新建账本 → 填写账本名称 → **选择账本类型**（共享可见型 / 分账结算型）→ 选择账本基准币种 → 创建成功，创建者成为 owner。
2. 在账本列表可切换查看不同账本、设置某个账本为"默认账本"。
3. owner/admin 可编辑账本名称、管理成员、管理分类；owner 可解散账本。

**账本类型说明（面向用户的差异）**：

- **共享可见型**：多人往同一账本记账，能看到"每笔是谁记的、谁付的"，但系统**不计算**成员之间的欠款。适合家庭共同开支等"只记录不算账"的场景。
- **分账结算型**：在记录时可指定"谁付的钱、由哪些人分摊"。P1 仅记录这些信息；**自动算清谁欠谁**的功能在 P2 提供。适合 AA、合租、旅行拼账。

**规则**：

- 首次使用必须先创建一个账本，之后进入的即为默认账本。
- 账本类型在创建时选定；P1 内类型是否允许创建后修改——列入待定问题。
- 删除/解散账本前需二次确认，并提示该操作不可恢复。

**验收标准**：

- 能创建两种类型的账本并正确标记类型。
- 关闭再打开小程序，进入的是默认账本。
- 切换默认账本后再次进入，生效。
- 非成员无法通过任何入口看到未受邀账本。

---

### 4.2 记账录入

**用途**：以最少的操作记录一笔收入或支出。

**操作流程**（记一笔）：

1. 点击"记一笔" → 进入录入页。
2. 选择 **收入 / 支出**（默认支出）。
3. 输入 **金额**，选择 **币种**（默认跟随用户当前**展示币种**，与看账口径一致；取不到其汇率时回退账本基准币种，可改）。
4. 选择 **分类**（一级 → 二级，九宫格快选）。
5. 可选：填写 **备注**、上传 **图片附件**、选择 **日期**（默认今天）。
6. 在共享/分账账本中，可指定 **记录人**（默认本人）与 **付款人**；分账结算型可进一步指定分摊成员（P1 仅记录）。
7. 保存 → 返回列表，新记录出现在顶部。

**规则（轻量化）**：

- 尽量带默认值减少点击：默认上次使用的账本、今天、当前展示币种、常用分类靠前。
- 录入页在账本/汇率/分类数据就绪前显示整页加载遮罩，禁止交互，避免加载中误操作导致状态错乱。
- 一笔记录包含：类型（收/支）、金额、币种、当日固化汇率、分类、日期、备注、图片、记录人、付款人（分账时含分摊信息）。
- 图片附件支持添加多张（数量上限列入待定，建议默认 3–9 张）。
- 只读成员看不到"记一笔"入口。

**验收标准**：

- 3 步内可完成一笔最简记录（金额 + 分类 + 保存）。
- 保存后立即在列表可见，金额、分类、币种、记录人显示正确。
- 读写成员可编辑/删除自己的记录，不能编辑他人的；admin/owner 可编辑全部。

---

### 4.3 多币种

**用途**：允许一个账本里混着记不同货币的账，但统一用一种货币来看总数和图表。

**操作流程 / 表现**：

1. 记录时选择该笔的原始币种（如美元）。
2. 系统按**记账当天的汇率**把它换算成账本/展示币种，并**把这个换算结果固定下来**。
3. 列表与图表统一按用户所选的**展示币种**显示金额与汇总。
4. 单笔详情里可同时看到"原始币种金额"和"换算后金额"。

**规则（关键）**：

- **历史金额不随汇率变动**：一笔过去的外币记录，其换算金额在记账当天就已固定，之后即使汇率变化也不再改变，保证历史报表稳定、可复现。
- 汇率来源为**公开汇率数据（每日更新）**，界面需注明"汇率仅供记账参考，与银行实际结算可能有差异"。
- 当天汇率暂时取不到时，采用最近一次可用汇率并做标注（提示"汇率为最近一次数据"）。
- 用户可在设置里更改展示币种，更改后所有列表与图表按新展示币种重新汇总显示（各笔仍按其记账日汇率换算）。

**验收标准**：

- 用不同币种各记一笔，列表与图表汇总的展示币种一致且数值正确。
- 更改展示币种后，历史每一笔的"当日换算逻辑"不变，仅展示单位切换。
- 单笔详情能同时显示原始金额与换算金额。

---

### 4.4 分类管理

**用途**：为收支归类，便于统计。

**操作流程**：

1. **新建账本时自动生成一套默认两级分类**（收入/支出各一套），无需手动配置即可开始记账。
2. **记账时可随手增删分类**：在录入页一级九宫格 / 二级列表末尾点"添加"新增（可选图标），长按分类"停用"删除；均即时生效并持久化到本账本。
3. 重命名、调整顺序由 owner/admin 进行。

**规则**：

- 分类为**账本级**：每个账本维护自己的一套分类，账本内成员共用，保证统计口径一致。新建账本会**注入默认分类**，之后各账本独立增删，互不影响。
- 分类分两级；收入与支出各有各的分类集合。
- **新增 / 停用分类**：读写及以上成员（rw / admin / owner）均可，方便记账时即时增删（停用为软删）；**重命名 / 排序**限 owner/admin。
- 停用而非硬删：历史记录仍显示原分类名，保证历史统计口径稳定。

**验收标准**：

- 新增的自定义分类立即可在记账页选择。
- 同一账本不同成员看到的分类一致。
- 停用分类后，历史记录仍正确显示该分类名称。

---

### 4.5 图表统计

**用途**：把当前账本的收支情况可视化，开箱即用又可自定义；图表不止于看，可点击钻取到明细。

**渲染**：统一基于 **ECharts**（小程序 canvas 2d 定制包，仅含柱状/饼图等所需模块），点击、高亮、tooltip、过渡动画为内置能力。「添加图表」面板中的示意缩略图为静态图片，不属于图表体系。

**卡片类型（七种；添加面板按图形形态分组展示：环形 → 柱状 → 折线；同类型可重复添加、各自独立配置）**：

*环形图 · 占比*

1. **月度收支**——某月收入、支出、结余。卡片头部有月份切换器（`‹ 2026年7月 ›`：箭头逐月翻、点中间弹原生月份选择器，可选范围 = 建账首月 ~ 当月）。**不选月 = 永远跟随当前月**（默认卡行为）；选定具体月则固定显示该月。
2. **分类占比（含排行）**——某月支出（或收入）按**顶级分类**的占比环形图 + 分类排行列表（色点、分类名、金额、占比、笔数；前 5 名 + "全部 N 个分类"展开）。卡内有收/支切换（默认支出）与月份切换器（规则同月度收支卡）。
3. **年度收支**——双模式可切：**自然年**（`‹ 2025 年 ›` 年份切换器，范围 = 建账首年 ~ 当年，当年 = 跟随）/ **近一年**（滚动 12 个月）。
4. **账本累计收支**——自建立以来总收入、总支出、结余。

*柱状图 · 对比*

5. **收支对比**——近 N 月收入 vs 支出并排柱状（N = 6/12 可切）。
6. **收支柱状**——单指标柱状，卡内双开关：**指标**（支出 / 收入 / 结余）×**区间**（近 7/14/30 日、近 6/12 月）。

*折线图 · 趋势*

7. **收支趋势（折线）**——面积折线，卡内双开关同上（指标 × 区间）。收入按月、支出按日、结余走势等组合均由此覆盖，不再拆分成多个卡片类型。

**点击钻取（各卡 → 记录筛选列表页）**：

| 点击位置 | 跳转目标 |
|---|---|
| 分类占比卡：排行列表行 | 该月 × 该分类 × 该类型的明细 |
| 分类占比卡：环形图扇区 | 扇区高亮放大并联动排行列表（不直接跳转） |
| 月度/年度收支卡：收入/支出图例行 | 该月/该年（或近一年）× 该类型全部明细 |
| 收支趋势 / 收支柱状：某个数据点 | 两段式——先高亮并显示该日/该月金额与"查看明细"入口条，再点入口进明细（支出/收入带类型筛选，结余合看） |
| 收支对比：某月柱组 | 两段式，同上，进该月明细 |
| 累计收支卡：收入/支出图例行 | 全账本该类型明细 |

**记录筛选列表页**：标题呈现筛选条件（如「2026年7月 · 餐饮 · 支出」）；列表复用首页记账行样式，按天分组、分页加载；点行进记录详情页。

**操作流程**：

1. 进入统计页，默认看到四张互不重叠的默认卡片（月度收支、分类占比、支出趋势折线、收支对比）。
2. 顶部「＋ 添加」或列表底部「添加图表」打开带示意图的选择面板（可滚动，容纳全部类型；编辑模式中也可添加）。
3. **长按进入编辑模式后拖拽排序**，调整卡片先后顺序；编辑模式可删除卡片。
4. 退出编辑，布局自动保存。

**规则**：

- 所有图表**只统计当前账本**。
- 卡片布局与每张卡的配置（类型、顺序、所选月份、区间 N、收/支维度）**每个用户各存一套**，跟随"账本 + 用户"；某人的摆放不影响其他成员。
- 下次进入同一账本时，恢复该用户上次的布局。
- 金额按用户当前展示币种显示；分类聚合与钻取列表的换算口径与其他图表**完全一致**（按记录当日汇率）。
- 分类占比按**顶级分类**归并（子分类计入其顶级）；停用分类的历史记录仍计入原分类。

**验收标准**：

- 首次进入即显示四个默认卡片。
- 增删、拖拽排序、改月份/区间/收支维度后退出再进入，布局与配置保持一致。
- A、B 两位成员各自调整布局，互不影响。
- 切换账本后显示对应账本、该用户自己的布局。
- 月度收支卡选任意历史月，数字与该月记录逐笔核算一致。
- 分类占比各分类金额之和 = 该月该类型总额；每个钻取入口进入的明细列表合计与卡片显示的数字一致。
- 图表点击有高亮/动画反馈；弹层、tabBar 与图表无层级冲突。

---

### 4.6 AI 助手

**用途**：用对话方式帮用户查数据、读收据记账、用一句话记账，降低使用门槛。

**三项能力**：

1. **数据问答**：用户提问（如"上个月餐饮花了多少""这周谁花得最多"），AI 给出基于该账本真实数据的回答。
2. **收据识别记账**：上传收据/小票图片，AI 自动识别商家、金额、币种、日期并**建议分类**，生成一条**预填记录**。
3. **自然语言记账**：用户输入一句话（如"昨天打车 35"），AI 解析成一条**预填记录**。

**操作流程（收据识别为例）**：

1. 进入 AI 助手页 → 上传收据图片。
2. AI 返回识别结果，自动填入记账表单（金额/币种/日期/分类建议等）。
3. 用户**核对并可修改** → 确认 → 入账。

**规则（重要）**：

- **AI 绝不直接自动入账**：无论收据识别还是自然语言记账，都会生成**预填记录让用户确认后才保存**（识别可能出错）。
- 数据问答的答案基于当前账本真实数据，不允许编造数字；数字与账本实际一致。
- **AI 会话记录**：按「账本 × 用户」**完整保留**，不做滚动删除。
- **用量额度**：免费用户累计 **50 次**真实 AI 调用（数据问答 / 一句话解析 / 收据识别各计 1 次；后台配置，不在设置页暴露）。额度用尽后：问答给出明确提示，一句话记账自动降级为本地关键词解析（仍可用）；付费解锁在后续版本接入。
- AI 能力受当前账本与用户可见范围约束；只读成员可用问答，但不产生入账（因其无记账权限）。
- 面向用户明确告知："AI 分析基于你的账本数据进行"，保障知情与信任。

**验收标准**：

- 提一个可核对的问题（如某月某分类合计），AI 答案与手工核对一致。
- 上传一张清晰收据，能生成预填记录，且确认前不入账。
- 一句话记账能生成正确的预填记录。
- 免费额度用尽后，问答给出明确提示、不再产生模型调用；一句话记账降级为关键词解析仍可用；会话历史完整可见。

---

### 4.7 导入导出

**用途**：备份、迁移、把数据交给他人或其他工具分析。

**导出**：

- **JSON**：完整数据，用于备份与迁移。
- **Excel（含 CSV）**：表格形式，便于自行分析。
- **PDF**：便于阅读/打印的对账单或报表。

**导入**：

- **JSON**：恢复此前的备份。
- **Excel（含 CSV）**：从表格批量导入。
- **PDF 不支持导入**（仅用于导出）。

**规则**：

- 导出范围可选（当前账本 / 指定时间段）。
- 导入需给出结果反馈（结果面板：成功 / 跳过(重复) / 失败 / 新建分类，附失败与跳过明细及行号，可复制失败明细）。最近一次结果在本机保留：关闭面板后点「导入数据」行的摘要可随时重看，撤销在重看时仍可用；已撤销的批次重看时标明「已撤销」。
- **导入查重（数量对齐）**：指纹 = 日期+收支+金额+币种+标题+备注；每个指纹只导入「文件条数 − 账本已有条数」。同一文件重复导入幂等、与手动录入重复的行自动跳过、文件内合法的重复记录不误伤。跳过明细在结果面板展示。
- **撤销导入**：每批导入的记录带批次号，结果面板可一键「撤销本次导入」（删除该批次全部记录，不影响其他数据）。rw 成员只能撤销自己的批次，admin/owner 可撤销任意批次。
- 批量导入的"列映射/分类币种映射向导"属于 P3+ 增强；P1 提供基础导入即可（导入要求文件符合导出的标准格式）。

**验收标准**：

- 三种格式均能成功导出并正常打开。
- 导出的 JSON 能重新导入且数据一致；**紧接着再导一次同一文件，全部行被跳过、账本记录数不变**。
- 撤销本次导入后，该批次记录全部消失，其余记录与统计不受影响。
- 上传 PDF 作为导入时被明确拒绝并给出说明。

---

### 4.8 设置

**用途**：集中管理个人偏好与账本相关配置入口。

**包含项**：

- 展示币种设置。
- 分类管理入口（进入当前账本的分类管理）。
- 导入 / 导出入口。
- 账本管理入口（新建、切换、设默认、成员管理）。
- 意见反馈入口（提交反馈 / 我的反馈，有新客服回复时显示红点）。
- 关于/帮助、隐私说明等。

**验收标准**：

- 修改展示币种后，列表与图表即时按新币种显示。
- 各入口能正确跳转到对应功能。

---

### 4.9 用户反馈

**用途**：用户就问题/建议联系客服，并可跟踪处理进度。以**轻量工单**实现（反馈落云数据库），不做完整站内信，也不以真实邮件为载体。

**功能规则**：

- 提交内容：标题、内容（均必填），图片附件（≤ 3 张，存云存储），联系邮箱（选填，仅作兜底联系方式）。
- 工单模型：状态三档「待处理 / 处理中 / 已解决」；用户与客服的往来以**回复线程**形式追加在同一工单内，双方均可继续回复。
- 客服身份（两级）：**owner** 固定在云函数环境变量 `FEEDBACK_OWNER`（唯一权力源，应用内任何操作不可增删）；**admin** 由 owner 生成**一次性邀请码（24 小时有效）**邀请加入，仅 owner 可移除，admin 之间不可互删。owner 与 admin 都能看到**全部工单**、回复、修改状态；仅 owner 能进入「客服团队管理」（邀请/移除）。所有权限校验在云函数端完成，非客服只能读写自己的工单。
- 用户侧：「我的反馈」列表展示自己的工单与状态；有未读客服回复时，设置页入口显示红点。
- 通知：P1 不做主动通知（用户进入小程序查看）；P2 起客服回复通过微信订阅消息推送。客服邮箱 `suixinji.cs@outlook.com` 仅作为对外公布的联系渠道，P1 不从系统发送真实邮件。

**验收标准**：

- 标题或内容为空时不能提交，有明确提示；提交成功后「我的反馈」列表立即出现该工单，状态为「待处理」。
- 非管理员无法查看或回复他人工单（服务端强制，构造请求也不行）。
- 管理员回复并改状态后，用户端详情页能看到完整回复线程与最新状态；设置页入口出现红点，查看后红点消失。

---

## 五、前台页面说明

> 仅描述"页面用途、页面上有哪些区块与按钮、点击后的表现、页面间如何跳转"，不含界面美术与技术细节。

### 5.1 首次引导页

- **用途**：新用户首次打开时，引导建立第一个账本。
- **区块与操作**：欢迎说明；"创建账本"按钮 → 填写账本名称、选择账本类型（共享可见 / 分账结算）、选择基准币种 → 完成后进入首页。
- **跳转**：完成后进入"首页"，该账本成为默认账本。

### 5.2 首页

- **用途**：日常使用的主入口，看账、记账、切账本。
- **区块与操作**：
  - 顶部：**当前账本名称**（点击可切换账本）、**展示币种**标识。
  - 概要条：本月收入 / 支出 / 结余（按展示币种）。
  - **记账列表**：按时间倒序展示记录，每条显示分类、备注摘要、金额（展示币种）、原始币种标识、记录人；点击某条进入"记录详情"。
  - 悬浮 **"记一笔"** 按钮（只读成员不显示）。
  - 底部导航：首页 / 统计 / AI 助手 / 我的（设置）。
- **跳转**：记一笔→录入页；点记录→详情页；点账本名→账本切换；底部导航切换主页面。

### 5.3 记账录入页

- **用途**：新增或编辑一笔记录。
- **区块与操作**：收/支切换；金额输入 + 币种选择；分类九宫格（一级→二级）；日期（默认今天）；备注输入；图片上传；记录人/付款人选择（共享/分账账本，分账可选分摊成员）；保存 / 取消。
- **跳转**：保存后返回来源页（首页或详情），列表刷新。

### 5.4 记录详情页

- **用途**：查看单笔完整信息。
- **区块与操作**：显示全部字段（含原始金额与换算金额、记录人、付款人、图片、备注）；根据权限显示"编辑""删除"按钮。
- **跳转**：编辑→录入页（带入原值）；删除→二次确认后返回列表。

### 5.5 统计页

- **用途**：查看当前账本的图表仪表盘（ECharts 渲染，可点击交互）。
- **区块与操作**：卡片列表（默认四个，可加分类占比卡）；月度类卡片带月份切换器；分类占比卡带收/支切换；"添加图表"弹出带示意图的选择面板；长按进入编辑模式可拖拽排序、删除；退出编辑自动保存布局与卡片配置（每人各自一套）。
- **跳转**：账本切换后展示对应账本与该用户自己的布局；各卡点击钻取 → 记录筛选列表页（见 4.5）→ 记录详情页。

### 5.5b 记录筛选列表页（钻取落地页）

- **用途**：呈现某筛选条件下（月份 / 分类 / 收支类型 / 日期）的记录明细。
- **区块与操作**：标题区显示筛选条件；按天分组的记录列表（样式同首页），分页加载；点行进详情页。
- **跳转**：来源为统计页各钻取入口；返回回到统计页且滚动位置不丢。

### 5.6 AI 助手页

- **用途**：对话式查数据、读收据、一句话记账。
- **区块与操作**：对话区（历史会话，受上限约束）；输入框（发文字）；上传图片（收据）；当 AI 生成"预填记录"时，以**确认卡片**形式展示，用户可修改后"确认入账"或"放弃"。
- **跳转**：确认入账后，记录进入当前账本并可在首页列表看到。

### 5.7 账本管理页

- **用途**：管理所有账本与成员。
- **区块与操作**：
  - 账本列表：每个账本显示名称、类型、我的角色；可"设为默认""切换"。
  - "新建账本"按钮。
  - 进入某账本 → **成员管理**：成员列表及其权限；owner/admin 可"邀请成员"（通过微信分享邀请）、"移除成员"、"修改权限"；owner 可"任命/取消 admin""解散账本"。
- **跳转**：新建→引导式创建；成员管理→各操作就地完成并刷新。

### 5.8 我的 / 设置页

- **用途**：个人偏好与各功能入口集中地。
- **区块与操作**：展示币种设置；分类管理入口；导入/导出入口；账本管理入口；意见反馈入口（有新回复时带红点）；关于/隐私说明。
- **跳转**：各项点击进入对应页面或弹层。

### 5.9 意见反馈页

- **用途**：提交反馈、查看自己工单的处理进度与客服回复。
- **区块与操作**：
  - 提交表单：标题、内容、图片（≤ 3 张）、联系邮箱（选填）→ 提交成功进入「我的反馈」列表。
  - 我的反馈列表：每项显示标题、状态（待处理/处理中/已解决）、时间，有未读回复的项带红点；点击进详情。
  - 工单详情：完整回复线程（用户与客服往来），底部可继续回复追问。
  - 客服视角（owner/admin）：列表展示**全部**工单，详情内可回复并修改状态。
  - owner 额外可见「客服团队管理」入口：查看 owner/客服名单、生成一次性邀请码（24 小时有效）、移除客服。
  - 非客服用户在列表页底部可通过「收到客服邀请码？」输入邀请码成为客服。
- **跳转**：由「我的 / 设置页 → 意见反馈」进入。

---

## 六、关键流程（文字步骤）

### 6.1 记一笔（最简）

打开小程序 → 默认账本首页 → 点"记一笔" → 选支出 → 输金额 → 选分类 → 保存 → 回到列表看到新记录。

### 6.2 多币种记录到展示

记账时选外币（如美元）并输入金额 → 系统按当天汇率换算并固定 → 列表/图表统一用展示币种显示该笔换算后的金额 → 日后汇率变化，这笔历史金额不变。

### 6.3 上传收据到确认入账

进入 AI 助手 → 上传收据图片 → AI 识别并生成预填记录 → 用户核对、按需修改 → 确认 → 记录入账，出现在首页列表。

### 6.4 邀请成员加入账本

owner/admin 进入目标账本的成员管理 → 点"邀请成员" → 通过微信分享邀请 → 对方接受后加入该账本 → 管理员为其设置权限（读写/只读）→ 该成员即可在自己的小程序里看到并进入这个账本。

### 6.5 提交反馈到收到回复

设置页 → 意见反馈 → 填写标题与内容（可附图）→ 提交 → 「我的反馈」出现新工单（待处理）→ 客服在管理员视角回复并标记状态 → 用户再次进入小程序，设置页入口带红点 → 打开详情看到回复线程，可继续追问，直至「已解决」。

---

## 七、待定问题清单

> 以下为尚未拍板、可后续再定的点，不影响 P1 主体推进。

1. 账本创建后**是否允许修改账本类型**（共享可见 ↔ 分账结算）？建议不允许，或仅在无记录时允许。
2. 图片附件**单笔数量上限**（建议默认 3–9 张）。
3. 账本 **owner 所有权转让**功能是否放入 P1（建议 P2）。
4. AI 会话上限默认 50，是否需要按账本单独可调、或全局统一。
5. 展示币种是**全局一个**还是**每账本一个**（建议每账本可各自设一个展示币种，全局给一个默认）。
6. 导出的时间范围与字段是否需要更细的筛选（P1 先给"当前账本/时间段"两种）。
7. 只读成员是否可用 AI 数据问答（建议可用，但不产生入账）。
8. 反馈工单是否需要用户侧「确认解决 / 未解决」关单动作（建议 P2 随订阅消息一起做，防止客服单方面关单）。
9. ~~客服管理员邀请机制的触发时机~~（已拍板并实现：owner/admin 两级 + 一次性邀请码，见 4.9）。
10. 订阅消息模板从公共模板库选型时，需确认模板类目与小程序服务类目匹配（选定后把模板 ID 记入云函数环境变量）。

---

*（文档结束。P1 范围以第二节"功能范围与分期 · P1"为准；P2、P3+ 内容已记录，供后续按序推进。）*
